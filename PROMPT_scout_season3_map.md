# COMMISSION — SEASON 3: the full map. Two tracks, nothing discarded.

Convergent session. Don's rulings on your batched questions, binding:

1. **Sell side: EVERYTHING in scope.** No structure class is excluded. The existing law does
   the filtering: every entry argues past its closure tag or is an unchanged re-run and says
   so. One addition his ruling requires: any FORWARD book selling premium must model
   assignment and margin explicitly in its declaration, or its results overstate — build that
   into the fleet convention, not into scope limits.
2. **Forward-first is the DEFAULT.** The live paper apparatus is the home for every idea that
   can live there; the freeze is reserved for what only it can answer.
3. **Data: free tier now** (catalyst calendars scraped forward). **Index chains pre-authorized
   to ~$150/mo IF a registered question names them** — you name the question, Don confirms the
   purchase.
4. **NO CUT-TO-BUDGET. Nothing is discarded.** The map orders EVERYTHING: the brainstorm's
   full list, the top-10, every PARKED / DEFERRED / reserve item from Seasons 1-2 and the
   audits (MB2, O-4, O-5, E-7, E-8, MB15-SLIM, SC-1b leftovers, the works — sweep the ledger
   for every non-terminal status). Only two exclusion types exist and both are "cannot," never
   "shouldn't": (a) unchanged re-runs of closed designs; (b) unpowered-by-construction, shown
   with the MB22 arithmetic per item. Every exclusion is individually justified in writing and
   PARKED WITH A CONDITION, not erased. Instead of cut lines, deliver the CUMULATIVE COST
   CURVE: as the backtest queue executes in your proposed order, what N becomes and what every
   standing bar becomes at each step — so Don watches the price accrue rather than approving a
   cap he cannot see past.

## Deliverables

**1. `SEASON3_MAP.md` — the dependency map.** Every idea from `OPTIONS_BRAINSTORM.md` plus the
swept-in parked items, each classified:

- **TRACK F (forward fleet)** — lives as a declared paper book. For each: the declaration
  sketch (entry rule, structure, universe, sizing, what gets recorded), what it depends on,
  and its verdict horizon (when does it have enough fills to say anything — be honest; V5
  needed 30 and has 3).
- **TRACK B (freeze-only)** — needs a backtest register. For each: nearest closure tag argued
  past, instrument dependencies, trial cost, and its place in the cost curve.
- **INSTRUMENTS** — the zero-trial builds the tracks need, with owner lanes. Known already:
  the FLEET HARNESS itself (a standing convention + recorder for many concurrent declared
  paper books — entry rules frozen at declaration, V5-style fill recording, one ledger row
  per book; this is the season's I-1 and everything in Track F waits on it), the catalyst
  calendar scraper (free tier), the assignment/margin model for short-side forward books.
  Find the rest.
- **DEPENDENCY GRAPH + EXECUTION ORDER** — instruments first, then Track F launches (cheap,
  parallel — the fleet can launch MANY books at once), Track B sequenced with the cost curve.
  Lane routing per item. Collisions named (which items share files/instruments and cannot run
  concurrently — the MA-dependency-map precedent).

**2. The register/declaration drafts** for whatever the map says executes FIRST — as many as
the first wave needs, executor-ready.

**3. The sweep proof.** A table of every non-terminal ledger status you found (PARKED,
DEFERRED, DESIGN-RECORDED, reserve, NOT-STARTED) showing where each landed in the map — so
"nothing discarded" is checkable, not claimed.

## Discipline

Same as ever: no outcome statistics; graveyard tags carried from the brainstorm; MDE arithmetic
(both vocabularies) wherever a backtest is proposed; the fleet convention must make a declared
forward book TAMPER-EVIDENT (declaration committed before first fill, append-only records —
the PT-WRITER machinery is the model and much of it is reusable). Batched questions at the end.
Push ritual: commit on your branch, tell Don the one-line push, verify the land next session.
