# Lazy Prices — 10-K/10-Q language-change dataset: coverage report

Generated by `python -m valuation.research.lazy_prices`. The dataset itself lives in `data/filings/lazy_prices.csv` and is **gitignored**, so this file is the committed record.

**What it is.** Year-over-year document similarity between a company's consecutive same-type filings (Cohen-Malloy-Nguyen). **HIGHER similarity = 'lazy' = the paper's bullish read** — orientation fixed in advance and not negated anywhere in the code.

**What it is NOT.** No IC, no keep/reject, not wired into the panel. The gated test (CPCV / held-out) runs later, on this dataset.

## Universe

- Tickers requested: **250**
- Not in SEC's ticker->CIK map: **0**
- Mapped but filing no 10-K/10-Q: **55** — `TSM, SPCX, SKHY, ASML, HSBC, NVS, RY, AZN, BABA, MUFG, TM, SHEL, ARM, NVO, SAP, BHP, UBS, TTE, TD, BUD, SMFG, RIO, BBVA, UL, BTI, SONY, ING, PDD, HDB, BMO, MFG, PBR, BP, SNY, SPOT, GSK, IBN, CM, EQNR, CNQ, BN, ITUB, BCS, LYG, E, NTES, NGG, CNI, SU, AMX, MFC, AEM, TRP, NWG, RELX`

  Mostly foreign issuers (20-F) and trusts/ETFs, which are structurally out of scope. **But check this list every run**: a holdco reorganization looks identical here. XOM resolved to a successor entity with zero 10-Ks while 42 filings sat under the predecessor CIK; it is fixed via `CIK_OVERRIDES` / `cik_overrides.json`, which fetch both CIKs and merge them into one history.
- Tickers with at least one scored pair: **195**

## Filings

| | count | |
|---|---:|---|
| 10-K/10-Q attempted (no amendments) | 7,843 | every one EDGAR listed for these tickers in the window |
| downloaded and parsed | 7,843 | 100.0% of listed |
| **scored (paired with a prior-year filing)** | **7,095** | 90.5% of parsed |
| parsed but unpaired | 748 | no same-form filing 270-450 days earlier — the first year of any ticker's history can never pair, by construction |

Failures by reason: `none`

By form: `{'10-Q': 5285, '10-K': 1810}`  |  Date range (availability dates): **2016-08-10 to 2026-07-31**

## Short histories — resolve these, do not ignore them

Median filings per ticker: **42**. Tickers under 60% of it: **12**.

Under 60% of the universe median. Usually a genuinely younger company (recent IPO), but a holdco reorganization splitting the history across two CIKs looks exactly like this — see CIK_OVERRIDES. BLK returned 7 of ~42 for that reason.

| ticker | filings | first | last |
|---|---:|---|---|
| SHOP | 6 | 2025-02-11 | 2026-05-05 |
| SNDK | 6 | 2025-03-07 | 2026-05-01 |
| BNS | 6 | 2016-01-29 | 2021-01-29 |
| BAM | 6 | 2025-03-17 | 2026-05-08 |
| GEV | 10 | 2024-04-30 | 2026-07-22 |
| CEG | 18 | 2022-02-25 | 2026-05-11 |
| APP | 21 | 2021-05-14 | 2026-05-06 |
| HOOD | 21 | 2021-08-18 | 2026-07-30 |
| ABNB | 22 | 2021-02-26 | 2026-05-07 |
| DASH | 22 | 2021-03-05 | 2026-05-06 |
| PLTR | 23 | 2020-11-13 | 2026-05-05 |
| SNOW | 23 | 2020-12-03 | 2026-05-29 |

## Section isolation (heuristic — Item headings)

- MD&A isolated on **92.5%** of scored pairs
- Risk Factors isolated on **79.0%** of scored pairs

A section that cannot be isolated is written as MISSING, never as an empty string (which would score as 'the company rewrote it completely').

## Mean similarity (sanity, not a result)

| measure | mean |
|---|---:|
| `cosine_tf` | 0.9771 |
| `jaccard` | 0.8284 |
| `cosine_tfidf` | 0.8799 |
| `mdna_cosine_tf` | 0.9565 |
| `risk_cosine_tf` | 0.9103 |

Rows computed against a thin (<100 document) point-in-time IDF corpus: **0** — the earliest rows have little corpus to draw on. Every row carries `idf_docs` so they can be filtered downstream.

## Caveats to carry into the IC test

1. **Survivor-only universe.** Today's large caps, so a first-pass IC is a survivors' IC. Not survivorship-free like the Sharadar panel.
2. **Today's ticker->CIK map** (SEC `company_tickers.json`) applied to history — the same caveat already recorded for the 13D map and the sector map.
3. **Section extraction is a heuristic** and was wrong once already (cross-references read as headings). Trust `cosine_tf` / `jaccard` on the FULL document first; the section measures are secondary.
4. **10-Q risk sections are heterogeneous** — often just 'no material changes', sometimes the full list. `risk_words` is on every row; filter on it.
5. **Stub filings.** 5 rows come from documents under 2,000 words — trust/subsidiary registrants filing boilerplate, which scores ~1.0 against last year's copy of the same boilerplate and so reads as maximally 'lazy' for a reason unrelated to the paper's mechanism. `n_words` is on every row; filter on it.
6. **Amendments (10-K/A) are excluded** and rows built from a complete text submission rather than the primary document are flagged `doc_source="full"` (they include exhibits and are not comparable to `primary` rows).

_Build time: 297.1s._
