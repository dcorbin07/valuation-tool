# RESEARCH_LOG — one row per pre-registered test, append-only

**Audit item M1.** Started 2026-08-03 while executing `VALQUO_EDGE_AUDIT.md`.

## Why this file exists

Every multiple-testing claim in this project is currently computed against a denominator of **8**
— the eight weight schemes `_deflated_sharpe` is handed. The external audit reconstructed roughly
**146 distinct pre-registered tests** across the handoff corpus. Those two facts cannot both
inform the same claim, and the smaller one is the one shipping in `BACKTEST_RESULTS.json`.

The purpose here is a single, honest trial counter that survives sessions, so that:

- `_deflated_sharpe` can be fed a real `N` instead of 8;
- Benjamini–Hochberg can be applied across the family of *equity* signal tests, the way
  `options_autopsy` already does across its 126 option features;
- the Harvey–Liu–Zhu adjusted hurdle can be quoted for the number of trials **actually run**.

At N ≈ 146, √(2·ln N) ≈ 3.16 — which is, near enough, the Harvey–Liu–Zhu hurdle of 3.0 that the
long-short *t* of 3.52 already clears. **That is a stronger claim than the current one precisely
because it is defensible**, which is the whole argument for maintaining this file.

## Status — read this before using the count

**The retrospective population is NOT done, and `N` is NOT wired into `_deflated_sharpe`.**

The audit's own instruction was to populate this from "section A of the ledger", the reconstructed
list of ~146 prior tests. **That section is not in `VALQUO_EDGE_AUDIT.md` and no such document is
in the working folder** — only the count is quoted, in four places. So the retrospective rows have
to be re-extracted from the handoff corpus (`HANDOFF_*.md`, `OPTIONS_*.md`, the roadmap files),
which is real clerical work and was not attempted in a session already carrying thirteen
corrections.

**Wiring a partial count would be worse than the current state, not better.** A denominator of 30
carries the same false precision as a denominator of 8 while looking like it was measured. The
current statistic at least now *labels itself* as an undeflated PSR when `sr0` collapses (audit
B9), which is an honest signal. Wiring `N` happens when the count is complete, in one step.

## Schema

One row per test. A test earns a row when its threshold was committed **before** its run — that
is what makes it a trial rather than an observation. Exploratory looks, smoke tests and
diagnostics do **not** get rows; they get no claim either.

| Field | Meaning |
|---|---|
| `id` | stable, never reused. `<domain><n>` or the audit's own item ID where one exists |
| `date` | ISO date the verdict was recorded |
| `domain` | `equity` / `options` / `unified` / `infra` — BH-FDR families are formed within a domain |
| `hypothesis` | what was predicted, in one line, in the direction predicted |
| `universe` | the exact set. "full 2,710-name panel", "187-name options book", "smoke test: 12 names" |
| `metric` | the statistic the verdict rests on |
| `threshold` | the bar, as committed **before** the run |
| `verdict` | `ADOPTED` / `REJECTED` / `NULL` / `INCONCLUSIVE` / `SUPERSEDED` / `FIXED` |
| `source` | the handoff or commit carrying the numbers |

`FIXED` marks a correctness repair rather than a hypothesis test. **`FIXED` rows do NOT count
toward `N`** — repairing a bug is not a search over the data, and inflating the denominator with
bug fixes would understate the evidence rather than overstate it.

**`SUPERSEDED` ROWS DO COUNT. Resolved 2026-08-06, session 7.** This paragraph used to end
"Only `ADOPTED` / `REJECTED` / `NULL` / `INCONCLUSIVE` rows are trials", which excluded
`SUPERSEDED` — and `research_log.py` has never implemented that, because `_parse` skips `FIXED`
and nothing else. The prose and the counter disagreed, and it was not academic: the void X3 run
carries `n=12`, so the reading decides whether equity `N` is 92 or 104. Session 6 hit the
discrepancy, took the harsher number, and referred the rule here rather than settling it while
looking at its own results.

**The counter is right and the prose was wrong.** `N` is a multiple-testing denominator, and what
inflates the best-looking result is how many times the data was searched — not how the search was
labelled afterwards. A superseded search still happened, and what it found still shaped what was
run next. `SUPERSEDED` judges the validity of a RESULT; it says nothing about whether the data was
interrogated. This is the same argument the module's own docstring already makes for counting
trials that were never pre-committed, applied consistently. It is also the conservative reading:
it makes `N` larger and every threshold harder to clear.

---

## Rows

| id | date | domain | hypothesis | universe | metric | threshold (pre-committed) | verdict | source |
|---|---|---|---|---|---|---|---|---|
| C7 | 2026-08-03 | infra | The auto-merge gate covering one suite of sixteen lets regressions reach production | 16 suites, 552 tests | all suites green | every suite must pass locally before it may gate | FIXED | `HANDOFF_edge_audit.md` |
| D10 | 2026-08-03 | infra | Sharadar's schema questions can be settled from the live entitlement before it lapses | live API key | 6 documented questions | answer or record the failure | ADOPTED | `HANDOFF_edge_audit.md` |
| D10-a | 2026-08-03 | equity | Restatements append a new ARQ row, so a datekey-deduplicated TTM window can double-count a quarter | full export, 197,265 ARQ rows | share of (ticker, reportperiod) groups with >1 datekey | any non-zero share is a defect | FIXED | `HANDOFF_edge_audit.md` |
| B1 | 2026-08-03 | options | The broad options universe feeds an adjusted close into option maths that requires the as-traded price | 5 call sites, 4 modules | code + entry-IV sanity band | correctness, no threshold | FIXED | `HANDOFF_edge_audit.md` |
| B3 | 2026-08-03 | options | Expiry is marked at a stale quote rather than intrinsic value | `options_fill.round_trip` | mark age vs settlement | correctness, no threshold | FIXED | `HANDOFF_edge_audit.md` |
| B9 | 2026-08-03 | equity | The "Deflated Sharpe" is an undeflated PSR because the eight trials are indistinguishable | 8 weight schemes | `sr0` vs Sharpe | `sr0` < 5% of SR = undeflated | FIXED (relabel only) | `HANDOFF_edge_audit.md` |
| B10 | 2026-08-03 | equity | `accruals_q` is computed as Sloan and silently overwritten with FCF/NI | full panel | which definition survives `build_frame` | correctness, no threshold | FIXED | `HANDOFF_edge_audit.md` |
| B12 | 2026-08-03 | equity | Every "800 largest names" result was an alphabetical slice | `WRDSProvider.universe` | sort key | correctness, no threshold | FIXED | `HANDOFF_edge_audit.md` |
| B14 | 2026-08-03 | equity | The delisting mask's coverage is measured and discarded, so a missed delisting is silent | full panel | `ended_early_unmasked` | correctness, no threshold | FIXED | `HANDOFF_edge_audit.md` |
| B15 | 2026-08-03 | options | The per-trade headline is gross of commission, contrary to its documentation | all options trades | `return_pct` | correctness, no threshold | FIXED | `HANDOFF_edge_audit.md` |
| B16 | 2026-08-03 | options | A dead exit module is the likeliest thing to be mistaken for the live exit logic | — | imports | correctness, no threshold | FIXED | `HANDOFF_edge_audit.md` |
| B18 | 2026-08-03 | equity | Negative enterprise value is read two opposite ways within one theme | full panel, ~0.70% of rows | sign convention | one convention across all three ratios | FIXED | `HANDOFF_edge_audit.md` |
| B19 | 2026-08-03 | equity | Every "Sharpe" in the results file is an information ratio versus zero | all books | `rf` passed to `risk_stats` | correctness, no threshold | FIXED | `HANDOFF_edge_audit.md` |
| B20 | 2026-08-03 | equity | `earnings_yield` switches numerator definition mid-cross-section | full panel | numerator basis | one definition throughout | FIXED | `HANDOFF_edge_audit.md` |
| B24 | 2026-08-03 | equity | `sanity_check` evaluates factors more than once, on different bases | full panel | scan-list duplicates | each factor once | FIXED | `HANDOFF_edge_audit.md` |
| B26 | 2026-08-03 | equity | A filing dated `as_of` is usable at that day's close | insider + grades | `searchsorted` side | exclude same-day | FIXED | `HANDOFF_edge_audit.md` |

**Trials counted toward `N` from this session: 1** (D10; the rest are `FIXED`). The pre-committed
thresholds for **R1**, **R2** and **R7** are recorded in `HANDOFF_edge_audit.md` Part 0 and become
rows when they run.

---

## How to add a row

1. Write the hypothesis and the threshold **before** the run, in the session handoff.
2. Run it.
3. Append one row here with the verdict — including, especially, the rejections and the nulls.
   A log that only records adoptions is a denominator of 8 with extra steps.
4. Never edit or delete a row. A superseded result gets a **new** row with verdict `SUPERSEDED`
   pointing at the one that replaced it.

---

## Rows — retrospective reconstruction (added 2026-08-05, audit M1)

**Counting rule (changed 2026-08-05, and the change matters):** a trial counts whether or not its
threshold was pre-committed. The original rule — "a row is earned when the threshold was committed
before the run" — is the right test for whether a RESULT is credible and the wrong one for a
multiple-testing DENOMINATOR. What inflates the best-looking result is how many times the data was
searched, not how well each search was documented. The old rule would have systematically
understated `N` and therefore OVERSTATED significance, which is the exact error M1 exists to fix.
`pre` records `yes` (threshold in writing beforehand) or `retro` (reconstructed from the record).
`FIXED` rows still do not count. `n=<k>` marks a pre-registered GRID of k cells counted as k.

| id | date | domain | pre | hypothesis | metric | verdict | n | source |
|---|---|---|---|---|---|---|---|---|
| P5-1 | 2026-07-30 | equity | yes | Zeroing `low_risk` improves the composite out-of-sample | held-out LS t + alpha, both directions | ADOPTED | n=1 | CLAUDE.md, HANDOFF_STATUS.md |
| P5-2 | 2026-07-30 | equity | yes | Zeroing `insider` improves the composite out-of-sample | held-out LS t + alpha, both directions | REJECTED | n=1 | CLAUDE.md |
| P5-3 | 2026-07-30 | equity | retro | `neg_asset_growth` earns its place in capital_discipline | signal IC t | REJECTED | n=1 | CLAUDE.md |
| P6-1 | 2026-07-31 | equity | yes | The edge survives realistic trading costs | breakeven one-way bps vs cost profile | ADOPTED | n=1 | CLAUDE.md |
| P6-2 | 2026-07-31 | equity | yes | TTM ROE/ROIC beat quarterly (smoothing beats recency) | signal IC t | REJECTED | n=2 | CLAUDE.md |
| P6-3 | 2026-07-31 | equity | yes | Median/MAD robust z-scores beat mean/sd | composite LS t | REJECTED | n=1 | CLAUDE.md |
| P6-4 | 2026-07-31 | equity | yes | momentum and institutional are redundant and should consolidate | composite LS t | REJECTED | n=1 | CLAUDE.md |
| P3 | 2026-07-29 | equity | retro | SF3 per-manager conviction predicts returns | signal IC t | REJECTED | n=3 | CLAUDE.md (sm_conviction/holders/breadth; breadth kept) |
| P4-1 | 2026-07-29 | equity | retro | F-Score adds to quality | signal IC t | ADOPTED | n=1 | CLAUDE.md |
| P4-2 | 2026-07-29 | equity | retro | Accruals add to quality | signal IC t | ADOPTED | n=1 | CLAUDE.md |
| P4-3 | 2026-07-29 | equity | yes | Classic anomalies replicate on this panel | signal IC t | REJECTED | n=4 | CLAUDE.md (reversal, idio-vol, MAX, low-vol) |
| P10 | 2026-08-01 | equity | yes | Sector-neutral ranking improves the composite | held-out LS t + alpha | REJECTED | n=1 | HANDOFF_sector_neutral.md |
| P10-b | 2026-08-02 | equity | yes | Sector-neutral replicates under the deployed weights | held-out LS t + alpha | REJECTED | n=1 | HANDOFF_sector_neutral.md |
| PEAD-1 | 2026-08-01 | equity | yes | `pead_car` predicts returns incrementally to momentum | residualized IC t | REJECTED | n=1 | HANDOFF_pead.md |
| PEAD-2 | 2026-08-01 | equity | yes | `pead_drift` predicts returns | signal IC t, coverage floor | REJECTED | n=1 | HANDOFF_pead.md |
| EV-PIT | 2026-08-03 | equity | yes | Pricing EV at the rebalance date improves the value theme | signal IC + composite | ADOPTED | n=1 | HANDOFF_ev_fix.md |
| 13F-LAG | 2026-07-28 | equity | yes | The 13F edge is a look-ahead artifact (fresher data = stronger) | LS t + Deflated Sharpe by lag | REJECTED | n=4 | CLAUDE.md (15/45/135/225d) |
| 13F-DEP | 2026-07-30 | equity | retro | The entire edge is the institutional theme | alpha and LS t with theme stripped | REJECTED | n=1 | CLAUDE.md |
| LAZY-1 | 2026-07-25 | equity | yes | Year-over-year 10-K language change predicts returns | NW t on long-short | REJECTED | n=28 | HANDOFF_lazy_prices.md (28-cell grid, measures x horizons) |
| LAZY-2 | 2026-07-26 | equity | retro | Lazy-prices signals survive as an IC on the main panel | signal IC t | REJECTED | n=6 | HANDOFF_lazy_prices_ic.md |
| X4 | 2026-08-02 | equity | yes | The book beats an investable ETF benchmark | total-return difference | INCONCLUSIVE | n=1 | HANDOFF_free_analysis.md |
| GROWTH-1 | 2026-07-20 | equity | retro | EV/Sales promotion improves the established value branch | composite metrics | REJECTED | n=1 | HANDOFF_growth_evsales.md |
| GROWTH-2 | 2026-07-21 | equity | retro | Growth-valuation calibration improves scoring | composite metrics | REJECTED | n=2 | HANDOFF_growth_calibration.md, HANDOFF_growth_valuation.md |
| XSECT | 2026-07-27 | equity | retro | Cross-sectional exit rules improve the book | book alpha | REJECTED | n=3 | HANDOFF_deep_xsection.md, HANDOFF_deep_exits.md |
| B21 | 2026-08-04 | equity | yes | A sector cap improves net alpha | net alpha across cap levels | NULL | n=4 | HANDOFF_edge_audit.md (none/25/30/40%) |
| R1 | 2026-08-05 | equity | yes | The headline survives FF5+MOM (the word "alpha" is earned) | NW t on the intercept | ADOPTED | n=1 | HANDOFF_r1.md, HANDOFF_edge_audit.md Part 5 |
| X2 | 2026-08-05 | equity | yes | The headline is stable across rebalance grids | spread of top-decile alpha | ADOPTED | n=7 | HANDOFF_edge_audit.md Part 4 (7 grids) |
| X7 | 2026-08-05 | equity | yes | The project's thresholds are calibrated against the pipeline's noise | placebo percentiles | REJECTED | n=1 | HANDOFF_edge_audit.md Part 4 |
| R10 | 2026-08-05 | equity | yes | The edge survives an investable benchmark | excess return + NW t | ADOPTED | n=3 | HANDOFF_edge_audit.md Part 5 (costed EW, cap-weighted, SPY) |
| OPT-ENTRY | 2026-07-22 | options | retro | The options entry signal beats a random-entry control | expectancy gap | REJECTED | n=1 | OPTIONS_BACKTEST_RESULTS.md |
| OPT-VRP | 2026-07-23 | options | retro | Variance-risk-premium selling is profitable net of costs | expectancy | INCONCLUSIVE | n=1 | HANDOFF_vrp.md, OPTIONS_VRP_RESULTS.md |
| OPT-AUTOPSY | 2026-07-24 | options | yes | Some option feature separates winners from losers | BH-FDR across the feature family | REJECTED | n=126 | HANDOFF_trade_autopsy.md (126 features, BH-corrected within family) |
| OPT-TERM | 2026-07-26 | options | yes | `term_slope` retention predicts option outcomes | retention floor + expectancy | INCONCLUSIVE | n=1 | R7_term_slope_retention_floor.md |
| OPT-GREEK | 2026-07-25 | options | retro | Greeks-based filters improve option selection | expectancy | REJECTED | n=4 | HANDOFF_greeks.md |
| R2 | 2026-08-05 | options | yes | The options entry signal beats a random-entry control, on B1-B15-corrected data | expectancy gap, date-block bootstrap | REJECTED | n=1 | HANDOFF_edge_audit.md Part 6 (gap -7.47pp, CI [-13.92, -2.43], sign-test z -2.907) |
| R3 | 2026-08-05 | options | yes | Options trades are not independent; clustered inference changes a verdict | design effect vs shuffled null | ADOPTED | n=1 | HANDOFF_edge_audit.md Part 6. **SCOPE NOTE appended 2026-08-06 (session 6):** the 1.848 / null p95 1.266 in this row is the PRE-CORRECTION 3,042-trade book. The corrected 3,885-trade book is **deff 2.2121 vs null p95 1.2037**, which is what `UNIVERSE_RESULTS.json` ships. Options *t* shrinks by sqrt(2.212) = 1.487x, not 1.36x. No verdict changes. |
| R7 | 2026-08-05 | options | yes | `term_slope` clears a properly-argued retention floor and its economic arm | G3a/G3b/G3c + MIN_LATE_GAIN | REJECTED | n=3 | HANDOFF_edge_audit.md Part 6 (floor passes; economic gain -1.12pp vs +5.00pp bar) |
| O20 | 2026-08-05 | options | yes | Point-in-time liquidity selection lowers the options headline | expectancy on the PIT-liquid subset | REJECTED | n=1 | HANDOFF_edge_audit.md Part 6 (it ROSE, +3.41% -> +4.82%; audit expectation refuted) |
| S5-3 | 2026-08-06 | options | yes | The old-vs-new options gap is spread, not signal dilution | mid-fill vs touch-fill toll, paired date-block | ADOPTED | n=1 | HANDOFF_edge_audit.md session-5 closeout item 3 (toll -8.28pp, replaces the void -6.59pp; 68% spread, not 100%) |
| S5-4 | 2026-08-06 | options | yes | The four B1-touching autopsy features are informative | per-feature p, both directions | REJECTED | n=4 | HANDOFF_edge_audit.md session-5 closeout item 4 (none informative; the passing direction SWAPS when B1 is repaired) |
| S5-5 | 2026-08-06 | options | yes | Published options statistics are single-control-seed safe | CI-endpoint movement as a share of CI width | ADOPTED | n=8 | HANDOFF_edge_audit.md session-5 closeout item 5 (7 of 8 safe; effective_n multi-seed by policy; no published boolean flips) |
| X3-VOID | 2026-08-03 | equity | yes | The 7-theme composite beats its best single signal (pre-B6 panel, retired bars) | top-decile alpha gain | SUPERSEDED | n=12 | data/free_analysis/ABLATION_RESULTS.json — 110-date pre-B6 panel, alpha +11.88%, 1.0pp bar below X7's 1.95pp noise floor. COUNTS toward N (12 of equity's 111); the earlier note here said it did not, which the counter has never implemented — schema corrected session 7 |
| X3 | 2026-08-06 | equity | yes | The 7-theme composite beats its own best single signal on the corrected panel | paired per-period alpha difference, CI95 excludes zero | NULL | n=8 | HANDOFF_edge_audit.md session 6 (+4.51%/yr, CI95 [-0.14%, +9.12%]; only the full arm clears X7's LS bar of 2.14) |
| U7 | 2026-08-06 | options | yes | Refusing alerts on bottom-composite-decile names lifts the options book | mean pnl_pct lift, date-block CI95 | REJECTED | n=3 | HANDOFF_edge_audit.md session 6 (lift -0.57pp / -1.04pp / -0.44pp; interaction vs control -0.08pp; bottom decile is the 3rd BEST) |
| S4 | 2026-08-06 | equity | yes | `growth` earns a place in WEIGHTS_ESTABLISHED | zeroing it must COST >=0.25 long-short t AND >=100bps alpha in BOTH held-out directions | NULL | n=1 | HANDOFF_signals.md (costs -0.263t/-0.48pp one way, HELPS +0.549t the other; direction-dependent) |
| S4b | 2026-08-06 | equity | yes | The SPECULATIVE branch is better WITHOUT growth | holdout_theme_validate, both directions | REJECTED | n=1 | HANDOFF_signals.md (zeroing growth there is `rejected` - no evidence to remove it) |
| S1a | 2026-08-06 | equity | yes | Dropping book_to_price from the established value branch helps | holdout_compare_panels, standing margins, both directions | REJECTED | n=1 | HANDOFF_signals.md (d_LS_t -0.207 / -0.079; value theme IC t RISES 0.84->1.57 while the composite falls) |
| S1b | 2026-08-06 | equity | yes | Swapping book_to_price for neg_ev_ebitda helps | holdout_compare_panels, standing margins, both directions | REJECTED | n=1 | HANDOFF_signals.md (d_LS_t -0.270 / +0.012; value IC t 0.84->1.37, composite LS t 2.836->2.741) |
| S2 | 2026-08-06 | equity | yes | cash_op_prof earns a place in the quality theme | coverage floor, then IC t >= 2.71 (X7 calibrated), then held-out gate | NULL | n=1 | HANDOFF_signals.md (cov 95.3%, median IC +0.0026, t +0.84; corr 0.27-0.44 so distinct yet uninformative; quality IC t 3.10->2.91) |
| LOO | 2026-08-06 | equity | yes | Choosing a theme to drop by its own leave-one-out effect on a decide half improves the composite on a held-out half | measure-half top-decile alpha gain and long-short t gain vs MIN_HOLDOUT margins (100bps, 0.25) | NULL | n=7 | HANDOFF_edge_audit.md session 7 (drop momentum: -1.30%/-0.706; drop capital_discipline: +0.20%/-0.201; different theme selected each direction; 4 of 7 arms change sign between halves) |
| B8 | 2026-08-06 | equity | n/a | holdout_theme_validate computed rule_fired and never read it, so its verdict was a both-halves stability check named as an out-of-sample confirmation | oos_verdicts added, verdicts semantics frozen | FIXED | n=1 | HANDOFF_edge_audit.md session 7 (neither shipped decision changes; low_risk confirmed_oos in 1 of 2 directions, not 2) |
| P4 | 2026-08-06 | infra | n/a | seed_book never sold names that left the exported book, so the paper index was an ever-growing union of everything ever held | departed names closed into paper_index_closed, not deleted | FIXED | n=1 | HANDOFF_edge_audit.md session 7 (45/45 paper-track tests; guard refuses to act on a truncated export) |
| SELRULE-GATE | 2026-08-07 | infra | yes | Cross-country co-movement can be measured and used to calibrate a sign test's critical count | design effect vs its own shuffled null (X7 method), `clustering_measurable` | ADOPTED | n=1 | valuation/edge/cross_country.py, HANDOFF_edge_audit.md session 9 (blocks=months, observations=countries; 4 tests pin rho=0 reproducing the exact binomial k=12, no false alarm on independent data, detection of planted co-movement, monotonicity of the bar in rho) |
| SELRULE | 2026-08-07 | equity | yes | A stability-first selection rule beats the incumbent decide-half argmax, measured on 16 held-out countries | paired per-country sign test at the co-movement-calibrated critical count | NO CONTRAST | n=5 | PREREG_session9_selection_rule.md; HANDOFF_edge_audit.md session 9. TWO independent kills: (a) the design is unreachable - clustering measurable on 10/10 arm-pairs, rho up to 0.484, n_eff 1.94-4.03 of 16, so critical k = 17 of 16 and even a unanimous 16/16 gives p 0.0546; the pre-registered 12/16 bar carries a true alpha of 28.7%, not 3.84%. (b) both rules select `size` on usa, so every paired difference is identically zero |
| HACFLOOR | 2026-08-07 | infra | yes | The placebo recorder must summarise the HAC statistic it already computes, so X7's long-short floor can be re-derived on the estimator the project actually quotes | placebo output schema carries long_short_tstat_nw and its p95 | ADOPTED | n=1 | scripts/placebo.py, PREREG_session10_hac_floor.md, HANDOFF_edge_audit.md session 10. quantile_backtest computed the Newey-West t on every draw since R9 and the writer dropped it, so the 2.14 floor was derived on the naive t while the shipped statistic became the HAC 2.620 - a bar and a number from different estimators. Calibration searches nothing, so equity N is unchanged; pinned by test_session10_the_placebo_writer_summarises_the_hac_statistic_it_computes |
| O16 | 2026-08-07 | options | yes | term_slope is a front-month IV level wearing a second name: its variance and its cross-section are carried by the atm_front leg, not by the ~60-DTE leg | identity arm decides - abs(Spearman(term_slope, atm_front)) and var(atm_front)/var(term_slope); predictive arm is Spearman IC vs pnl_pct for term_slope, -atm_front and the residual, each with a date-block (calendar-month) bootstrap CI95; no-new-data control ranks by -atm_front at term_slope's own 40.6% retention | INCONCLUSIVE | n=5 | STOPPED AT THE PRE-REGISTERED REPRODUCTION GATE, no verdict on the hypothesis. Recomputed term_slope matched the banked value on only 86.435% of 3,885 rows against a required 99%. Cause attributed, not guessed: the chain store is LIVE and 19.5% of the alerts' ticker-year files were re-mined after the book was banked; rows whose chain is untouched reproduce at 100.00% (3,127 of 3,127, zero exceptions) while re-mined rows reproduce at 30.47%. EXPLORATORY read on the 3,358 exactly-reproducing rows, carrying NO verdict: Spearman(ts, atm_front) -0.5405 CI [-0.576, -0.501], below the 0.60 distinct bar, so the committed rule WOULD have said IS DISTINCT; the residual of ts on atm_front predicts BETTER than raw ts (IC +0.0774 vs +0.0645, both CIs excluding zero) while atm_front alone predicts nothing (+0.0148, CI spans zero). SUPERSEDED BY THE 2026-08-08 RE-RUN (row O16-REFROZEN below), which delivers the verdict on a refrozen book. This row is KEPT and STILL COUNTS: the arms were run, and the exploratory read they produced is exactly why the re-run was not blind and is charged again. Thresholds committed in valuation/edge/options_signals_v2.py (O16_LEVEL_RHO 0.80, O16_LEVEL_VAR_SHARE 0.60, O16_DISTINCT_RHO 0.60); ambiguous is a NULL. Book = state_r2_corrected.pkl, 3,885 trades / 186 names / 118 months. Blocking reproduction gate: recomputed term_slope must match the banked value within 1e-6 on >=99% of rows or the study stops. If the raw feature's own IC CI95 spans zero the predictive arm is declared UNINFORMATIVE and carries no verdict weight - ruled in advance because two nulls cannot discriminate. |
| O24 | 2026-08-07 | options | yes | term_slope is an earnings calendar wearing a second name: front IV inflates mechanically before earnings, so the slope is a date offset rather than a vol-surface read | OLS term_slope ~ days-to-next-earnings bucket dummies, statistic is R2 (share of term_slope variance the calendar alone reconstructs); pre-committed direction Spearman(term_slope, days) > 0 with a date-block CI95 excluding zero, a significant wrong-sign slope refutes the mechanism whatever R2 says; no-new-data control keeps only alerts >30d from earnings | NULL | n=4 | R2 0.2144, date-block CI95 [0.183, 0.248] - the WHOLE interval sits below the committed 0.25 bar - and the pre-committed monotone direction test gives Spearman +0.0018, CI [-0.051, +0.055], spanning zero. The mechanism IS visible but LOCAL and NON-MONOTONE - mean term_slope is -0.1916 in the 0-7d bucket against roughly +0.01 to +0.02 at 8-30d and -0.031 at 61-120d - which is why a categorical R2 nearly clears while a monotone rank test is near-blind to it. The pre-committed direction test was the wrong SHAPE for the effect; it was chosen before any data was seen and the verdict stands as committed. No-new-data control runs the OTHER way: keeping only alerts >30d from earnings makes the book WORSE (-0.95% vs +3.81%, diff -4.76pp, CI [-7.59, -2.10], excludes zero). Thresholds committed in valuation/edge/options_signals_v2.py (O24_CALENDAR_R2 0.25, O24_DISTINCT_R2 0.10, O24_MAX_DAYS 120); ambiguous is a NULL. Earnings dates from EVENTS code 22, the PEAD study's own point-in-time source; coverage is PARTIAL (157/186 names, 3,495/3,885 alerts carry a forward date) and an alert whose next earnings is >120d away is excluded as UNKNOWN rather than scored as far-from-earnings - the scoping pass saw an apparent 3,004-day gap, which is a hole in the calendar and counting it would load the test toward the answer this lane finds convenient. RE-CHECKED 2026-08-08 on the REFROZEN feature (chain freeze, audit O16 follow-on) and the NULL is re-confirmed rather than assumed: identical eligibility (3,458 of 3,885, 157 names, 118 months), R2 0.21443 -> 0.21555 with CI95 [0.1840, 0.2498] still wholly below the 0.25 bar, direction +0.00183 -> +0.00579 still spanning zero, and every bucket mean moving by <=0.0014 (0-7d -0.19159 -> -0.19120). 13.6% of rows differ INDIVIDUALLY and the aggregate barely moves. NO NEW ARMS: this is a re-measurement of the same registered arms on repaired inputs, so it adds nothing to N. |
| MLCOMB | 2026-08-08 | equity | yes | A shallow gradient-boosted tree over the seven deployed theme z-scores beats the flat 1/7 linear composite out-of-sample | verdict-half top-decile alpha margin >= 1.95pp AND LS HAC t margin >= 0.25 AND tree's own LS HAC t >= 2.2837, in BOTH split directions | REJECTED | n=8 | PREREG_ml_combiner.md (blind, ec6c01d) + PREREG_session11_execution_protocol.md; HANDOFF_edge_audit.md session 11; data/free_analysis/ML_COMBINER.json. Worse on alpha in BOTH directions: decide-early/measure-late d_alpha -9.70pp, d_HAC_t -2.118; decide-late/measure-early d_alpha -5.48pp, d_HAC_t -2.877. THE TREE'S DECILES RUN BACKWARDS OUT OF SAMPLE (monotonicity +0.382 and +0.842, where negative is well-ordered) while the linear arm on the IDENTICAL rows through the IDENTICAL function is well-ordered (-0.903, -0.855) and the equal-weight benchmark is identical between arms - so the inversion is a property of the model, not the harness. Every one of the 16 grid x direction cells had a POSITIVE decide-half CPCV out-of-sample rank IC (+0.011 to +0.024), so the model generalises inside the decide half and reverses across the boundary. The two directions selected OPPOSITE ENDS of the grid - decide-early picked the most complex point (d3/lr0.10/it300), decide-late the least (d2/lr0.03/it100), with capacity helping monotonically in one half and hurting monotonically in the other. Equity N 121 -> 129, DSR 0.8628 -> 0.8556, sqrt(2 ln 129) 3.118. |
| CHAINFREEZE | 2026-08-08 | infra | n/a | The option-chain store is mutable, so a banked options verdict has no reproducible referent: `data/options` is re-mined in place and the authoritative book measured 86.435% reproducible against it, with 19.5% of its ticker-year files rewritten after banking | content-addressed byte fingerprints per symbol-year with a (size, mtime_ns) sidecar cache, trade-scope frozen row copies, and a replay pin that refuses a drifted read at theta_bulk's single load choke point; descriptive at bank time, blocking only for replays | FIXED | n=1 | HANDOFF_optionsbot.md 2026-08-08; valuation/edge/options_freeze.py, tests/test_options_freeze.py (42 tests) -- Design chosen on a MEASUREMENT the brief required before rejection: a trade-scope frozen copy of the R2 book costs 157.88 MB pickle / 27.44 MB gzip over 2,870,079 rows against a 26.98 GB store (0.585%), and the artifact actually banked is 23.30 MB over 2,870,811 rows (0.086%) -- so copying is ADOPTED, not rejected, because a book is SPARSE in the store (one day in ~250 per symbol-year). Fingerprinting adopted alongside it: the copy answers 'can this verdict still be checked', the fingerprint answers 'is what I read now what was read then'. Reconciliation across all ten banked books found drift is PROGRESSIVE and tracks AGE, not the book: everything banked 2026-08-03 sits at ~56% untouched, everything banked 2026-08-05 at ~80%. Nothing is lost (0 absent symbol-years) and the store has been quiet since 2026-08-06 04:29. Frozen: R2 corrected + its five controls; retired with annotation: pre-correction, state_mid, entry lab, exit lab. NOT COUNTED toward N -- this is instrument repair, not a search. |
| O16-REFROZEN | 2026-08-08 | options | yes | term_slope is a front-month IV level wearing a second name: its variance and its cross-section are carried by the atm_front leg, not by the ~60-DTE leg | register ad686 unamended (ad66468): identity arm decides via abs(Spearman(term_slope, atm_front)) against O16_LEVEL_RHO 0.80 and O16_DISTINCT_RHO 0.60 plus var(atm_front)/var(term_slope) against O16_LEVEL_VAR_SHARE 0.60; predictive arm is Spearman IC vs pnl_pct for term_slope, -atm_front and the residual with date-block (calendar-month) CI95; ambiguous is a NULL | IS DISTINCT | n=5 | HANDOFF_optionsbot.md 2026-08-08; data/options_freeze/R2_CORRECTED_2026-08-08/ -- THE OBJECT OF STUDY CHANGED AND IT IS DECLARED: the register's gate compares against the BANKED value, that comparison fails at 86.435% and can never pass because the drifted rows' inputs no longer exist, so the verdict is delivered on the REFROZEN book -- atm_front, atm_mid and term_slope recomputed together from one frozen store and therefore mutually consistent by construction. 3,885 of 3,885 rows, 186 names, 118 months, 0 errors, 0 drift, run under a replay pin re-verified afterwards at 1,429/1,429 clean. Spearman(ts, atm_front) -0.53966, CI95 [-0.5740, -0.5022], entirely below the 0.60 distinct bar. THE VERDICT HINGES ON THE PRE-REGISTERED CHOICE OF SPEARMAN AND MUST BE QUOTED WITH THAT: Pearson is -0.82793, which CLEARS the 0.80 level bar, so the same data under Pearson returns the OPPOSITE verdict (IS THE LEVEL). Variance shares front 1.88319 / mid 0.61088 / -2cov -1.49406 exceed 1 because the legs co-move (Spearman(front, mid) +0.79093) and are uninterpretable without the covariance term. Predictive arm informative (raw IC excludes zero, so the null-vs-null clause did not fire) and it CORROBORATES distinct: residual IC +0.07034 [+0.0287, +0.1131] beats raw term_slope +0.05673 [+0.0206, +0.0922] while -atm_front alone predicts nothing +0.01316 [-0.0333, +0.0626]. CHARGED AGAIN RATHER THAN WAIVED AS A REPAIR: last cycle's exploratory read on the 86.4% subset already showed this answer, so the re-run was not blind. Options N 164 -> 169. |
| M1-PARSE | 2026-08-08 | infra | n/a | The trial counter detects a FIXED verdict by searching the whole joined row, so any row whose free text contains the word "fixed" is silently dropped from N - understating trials and therefore OVERSTATING the significance of every DSR-gated claim | verdicts, the grid multiplier and the domain must each be read from their own column; pinned by a fixture the old parser gets wrong | FIXED | n=1 | PREREG_session12_recount.md; valuation/edge/research_log.py; test_session12_the_trial_counter_reads_verdicts_from_the_verdict_column_only. THE DEFECT IS REAL AND WAS NEVER LIVE: re-running both parsers over all TEN historical revisions of RESEARCH_LOG.md gives identical counts at every one, and no `fix*` word appears outside a verdict cell in any of the 72 data rows. Equity N stays 129, DSR 0.8556, sqrt(2 ln 129) 3.118 - no published number was ever wrong. Two sibling defects of the same class fixed with it: the `n=<k>` grid multiplier was grepped from the whole line and the domain was taken from the first cell matching any domain name. |
| X7RECON | 2026-08-08 | infra | yes | The 8-vs-7 `ls_t >= 2.0` gap between X7's placebo and session 10's re-run is caused by the two sweeps running at different project trial counts, because the CPCV adopt gate's haircut is floored at the research log's N and the adopted weights then feed quantile_backtest | name the specific draw and show it crosses 2.0 under one weighting and not the other, reproducible; a plausible story is explicitly NOT a diagnosis | ADOPTED | n=1 | PREREG_session12_recount.md section 6 (committed before the sweep); scripts/x7_reconcile.py; data/free_analysis/X7_RECONCILE.json (all 100 rows retained). SEED 1005, and it is the ONLY draw of 100 whose adopt decision differs between the two N. Margin 0.00287097 vs se 0.00094470: clears the N=84 bar of 0.0028122 and fails the N=121 bar of 0.0029257. Naive ls_t 2.1273 under the challenger's weights, 1.0454 under base; session 10's retained artifact records 1.0453572947436582, matching the base-weight recomputation to sixteen digits. Substituting the adopted value into session 10's 100 draws gives EXACTLY 8 at t>=2.0, which is X7's recorded figure; the adopt count at N=84 comes back 21, which is M1's recorded 21%; and the naive p95 stays 2.1437 with max 3.436, which is why session 10's control reproduced X7's percentiles to the digit while missing one draw - 2.1273 lands just below the 95th percentile. It also explains why the gap looked undiagnosable: seed 1005 did not drift across 2.0, it jumped 1.08 of a t because its weights changed, so "no draw near the boundary" was the wrong thing to look for. CONSEQUENCE THAT OUTLIVES IT: A CALIBRATED PLACEBO FLOOR IS A FUNCTION OF N. Here the floors did not move because the affected draw landed below the percentile, which is luck rather than design; every sweep must record the N it ran at and a floor may not be compared across sweeps run at different N without checking. The SHIPPED strategy is unaffected - it does not adopt, it keeps current-default, so no haircut touches its ls_t; the exposure is to the calibration, not the headline. Zero trial cost to equity, which stays 129; logged in infra on the HACFLOOR precedent. |
| O1 | 2026-08-08 | options | yes | an exit rule that beats the shipped +100%/-50%/half-DTE exit on the alert book is a property of the EXIT only if it also beats it on random entries drawn from the same universe and dates; if it works only behind the alert it is entry information leaking through the exit | the 21 policies of options_exitlab.POLICIES unamended, X1-X5 imported unchanged: ADOPT needs expectancy gain >= MIN_EXPECTANCY_GAIN 0.10 on signal AND > 0 on random, positive in both held-out halves on both sets, >= 30 trades, paired name-year sign advantage surviving BH-FDR at Q 0.10, and PBO < 0.50 by CSCV over the policy x time-block matrix; date-block (calendar-month) bootstrap 2,000 draws seed 0; the paired name-year SIGN TEST carries the verdict per the standing R2 rule; ambiguous is a NULL | REJECTED | n=21 | HANDOFF_optionsbot.md 2026-08-08; PREREG_o1_o23_exits.md at dc2c486 committed before any policy was scored; data/options_exitlab/EXITLAB_FROZEN_2026-08-08.json -- FIRST RESEARCH TO RUN ENTIRELY OFF THE CHAIN FREEZE, and both blocking gates passed: every contract history came from the frozen copy (3,885/3,885 signal, 29,785/29,785 random, zero live reads, freeze fingerprints 2,987/2,987 symbol-years clean) and the shipped policy replayed from it reproduced the banked book at 99.820% on the last_quote parity check and 100.000% (3,885/3,885) under honest settlement, the 7 residuals being exactly the settled_at_intrinsic trades the parity mode is defined to differ on. The replay independently reproduces R2's published headline to the digit: real +3.41%/trade vs control +10.06%, control per-seed range +6.46% to +15.34%. NO POLICY CLEARS THE GATE. tp100_only is the ONLY policy to clear the 10pp signal bar (+10.78pp signal, +17.86pp random, both clustered CIs excluding zero, positive in both halves on both sets, selected by the held-out chooser in all four directions, PBO 0.000) AND IT FAILS THE STATISTIC THAT CARRIES THE VERDICT: paired name-year sign z -5.76 on the signal book, winning 41.7% of 1,217 decided cells with a MEDIAN cell -5.79pp, because it converts total losses from 1.39% of trades to 46.87% while letting winners run (mean hold 14.5d -> 42.6d). The pre-registered direction rule assigned it p=1.0 rather than a small two-sided p, which is the guard working. On the RANDOM book the same rule genuinely wins - 64.5% of decided cells, z +10.55, median +11.13pp, stable across all five seeds (+15.9pp to +22.2pp) - so it is CONTROL-ONLY by the verdict-carrying statistic. THE STRUCTURAL FINDING: the exit changes that are BROAD are SMALL and the one that is LARGE is NARROW. tp150 and tp200 (raise the take-profit) are FDR discoveries with positive sign tests on BOTH entry sets (tp150 signal 66.2% of decided cells z +7.93, random 61.9% z +8.09) but gain only +3.19pp and +3.82pp, below the pre-committed bar; sl30 is the exact mirror, winning 69.9% of decided cells on signal (z +13.13) while LOSING 3.11pp of expectancy. Mean and median disagree systematically on a barbell payoff and which exit 'wins' depends entirely on which statistic is quoted. Superseding note: the 2026-08-03 exit lab REJECT is not cited as evidence - it ran on the pre-correction book (3,119 entries, 278 names, banked two days before B1 moved median entry IV 1.4200 -> 0.2497), with ~2 control draws rather than 5, and its paths were built through the pre-B2 quote filter this session found and repaired. |
| O23 | 2026-08-08 | options | yes | an option exit confounds the stock move with vol and theta, so an exit rule's P&L difference against the shipped exit is mostly the UNDERLYING moving rather than anything option-specific | policies share the identical entry and differ only in exit date, so per trade d_opt = R_opt(P) - R_opt(shipped) and d_und = the underlying's return over the same two holding periods; statistic is R2 of OLS d_opt ~ a + b*d_und, POOLED over all (trade, policy) pairs whose exit date differs from shipped's, with a date-block (calendar-month) CI95, 2,000 draws, seed 0. UNDERLYING-DRIVEN needs R2 >= 0.50 with the CI95 LOWER bound also >= 0.50; OPTION-DRIVEN needs R2 <= 0.25 with the upper bound also <= 0.25; anything else is a NULL. Verdict stated on the SIGNAL book and downgraded to NULL if the two entry sets disagree. Secondary Greek attribution carries no verdict | NULL | n=2 | HANDOFF_optionsbot.md 2026-08-08; PREREG_o1_o23_exits.md at dc2c486; data/options_exitlab/O23_UNDERLYING_2026-08-08.json -- NULL BY THE PRE-REGISTERED POOLED STATISTIC AND SET-DEPENDENT: signal R2 0.53304 CI95 [0.48564, 0.58428] over 26,851 pairs and 118 monthly blocks, so the point estimate clears 0.50 but the lower bound does not; random R2 0.55737 CI95 [0.53112, 0.61686] over 210,731 pairs CLEARS as UNDERLYING-DRIVEN. The register downgrades a disagreement to NULL and that is what is recorded. REPORTED RATHER THAN BURIED, because quoting only the NULL would mislead: the POOLED fit understates the per-policy relationship, since slopes range 6.36 to 17.45 across policies and a pooled regression over heterogeneous slopes loses R2. Per policy on the signal book the median R2 is about 0.70 and 17 of 20 policies exceed 0.50, ranging 0.4180 (tp100_only) to 0.7542 (dte21) - the policy with the largest expectancy gain is the LEAST explained by the underlying, which is what holding an all-or-nothing payoff to expiry does to a linear fit. The secondary Greek attribution agrees with the primary by a completely different route: of total absolute mark movement between the two exit dates, delta is 50.70%, gamma 15.57%, theta 14.07%, vega 13.25% and the linearisation residual 6.41% (23,983 pairs, 2,868 skipped, r 3%), so delta alone lands on the same ~50% the regression found and delta+gamma is 66.3%. Mean SIGNED contributions say the rest plainly: gamma +0.8528 and theta -0.7708 nearly cancel, leaving delta +0.4617 - holding a long call longer buys convexity and pays for it in decay at almost the same rate, and what survives is the direction of the stock. CAVEAT THAT MUST TRAVEL: the underlying closes come from data/bulk/prepared/bars, a DIFFERENT store that the options freeze does not cover; the bars consumed are fingerprinted per run but were never frozen. |
| ARTIFACT-N | 2026-08-08 | infra | n/a | The canonical BACKTEST_RESULTS.json ships a Deflated Sharpe computed at n_trials=84, a denominator 45 trials out of date, so a reader trusting the artifact over the brief gets the flattering number | the artifact's deflated_sharpe must equal research_log.detail()['n_used'] at the current N, with every other published figure unchanged | FIXED | n=1 | HANDOFF_edge_audit.md session 13. Full re-run on the 2,531-name / 69-date universe from a CLEAN tree at e83df30. DSR 0.8996589404135822 at n_trials 84 -> 0.855607566829599 at 129; n_trials_from_research_log 84 -> 129 and n_trials_source still reads RESEARCH_LOG.md (audit M1), confirming session 4's wiring; sr0_benchmark 0.4056 -> 0.4303; n_trials_from_weight_schemes stays 8 so the degrade path is intact. NOTHING ELSE MOVED: a leaf-by-leaf diff gives 15 moved / 32 added / 0 removed, of which five ARE the DSR chain and the other ten moved by 0.000% (last-digit float on costs and net_sharpe). long_short_tstat 2.8360640685320595, top_decile_alpha 0.07174142332098163, monotonicity -0.8909090909090909, universe 2531/69 and every benchmarks figure are bit-identical. The known-nondeterministic insider theme reproduced to sixteen digits. THE ARTIFACT WAS ALSO STALE IN A WAY NOBODY HAD NOTICED: it predated session 7's B8 repair and shipped NO oos_verdicts block at all, plus it predated the cash_op_prof signal - so 32 leaves are additions, not cosmetics. Both shipped decisions unchanged on the fresh run (low_risk confirmed_oos, insider rejected_oos) and the sanity layer fires its two expected flags, neither silenced. The replaced file carried git.dirty true at 4f41c9f; this one records a clean commit. No trial was run, so equity N stays 129. |
| PT-REGISTER | 2026-08-09 | equity | yes | The Valquo Index beats SPY on a FORWARD, out-of-sample paper track - monthly excess return over SPY is positive over the contracted horizon, measured on data that did not exist when the rules were fixed. This is the project's only test of the equity edge on data nobody had looked at | PAPER_TRACK_CONTRACT.md Option E, IN FORCE from this commit and unamendable. Bound source is the PUBLISHED VALQUO INDEX (data/valquo_track.json plus data/valquo_track_history.csv, read by valuation/screener/index_track.py), NOT the Tradier sandbox engine. Book is the top decile of the large-cap tier, score-weighted, 8% cap; benchmark SPY total return; statistic one-sided NW(3) t on monthly excess net of a modelled 0.14529 pp/month cost drag. SUPPORTED needs t >= +1.645 AND cumulative excess > 0 at 2031-07-30; UNSUPPORTED needs t <= -1.645; anything else is a NULL. Operational gate 2027-01-30 tests recording, not returns. Plus a pre-registered Robbins normal-mixture anytime-valid confidence sequence (sigma 3.9846917305386294 pp per month, rho 3, alpha 0.05 two-sided, cost drag 0.14529) first rendered 2027-01-30 and monthly thereafter, whose crossing is the ONLY legitimate early exit | REGISTERED | n=1 | HANDOFF_edge_audit.md session 14; PAPER_TRACK_CONTRACT.md sections 5 and 6; valuation/edge/track_meter.py; tests/test_track_meter.py (20 tests pinning every constant to a literal). VERDICT PENDING UNTIL 2031-07-30 - this row is logged at REGISTRATION, which is the point of a pre-registration, and the verdict is appended to it rather than replacing it. GENUINELY BLIND: at this commit the bound series holds two daily rows and ZERO complete calendar months, so no parameter could have been tuned to the outcome even in principle. THE POWER IS STATED IN ADVANCE AND IT IS POOR: information ratio 0.88/yr in-sample gives t=2.0 at 5.2 years, so the 60-month verdict has 49% power and NULL is the single most likely outcome even if the strategy is exactly as backtested. The anytime-valid meter is far weaker still by construction - measured over 40k simulated AR(1) paths its false-crossing rate is 1.5% against a nominal 5%, and its power at the backtested +9.99 pp/yr edge is 13.3% by 60 months and 30.7% by 120, needing about 19 pp/yr to cross at 60 months. A meter that has not crossed is therefore the EXPECTED outcome and is not evidence against the strategy. The AR(1) inflation of sigma (design effect 1.466091 from R9's lag-1 +0.189) is load-bearing and measured: without it the false-crossing rate is 6.7%, i.e. the naive version breaks its own guarantee. Sigma may never be revised downward - at 1.5x the assumed volatility the false-crossing rate is 20%. TWO DEFECTS DISCLOSED AT REGISTRATION, neither repaired here: the bound series has NO automated writer anywhere in this repository (four of six due rows missing, hand-maintained on the Cowork side), so the operational gate cannot pass until one exists; and a SECOND recorder (the Tradier sandbox engine) runs a DIFFERENT book of 10 equal-weighted names at 10% each, which this contract's own 8% cap forbids, from a different inception of 2026-08-03. Equity N 129 -> 130; charged rather than waived because the verdict will be quoted as a claim about the equity strategy, and understating N overstates significance. |
| ARTIFACT-N14 | 2026-08-09 | infra | n/a | Adding PT-REGISTER moves equity N 129 -> 130, so the canonical BACKTEST_RESULTS.json committed at c1b0ed6 now ships a Deflated Sharpe computed at the previous denominator - the same silent staleness session 13 repaired, re-created by this session's own row | the artifact's deflated_sharpe must equal research_log N at the current count, with every other published figure unchanged | FIXED | n=1 | HANDOFF_edge_audit.md session 14. Full re-run on the 2,531-name / 69-date universe from a CLEAN tree at f56cc51, git.dirty false. DSR 0.855607566829599 at n_trials 129 -> 0.8547321268980206 at 130; sr0_benchmark 0.43031816623094016 -> 0.43075197686098127; n_trials_source still reads RESEARCH_LOG.md (audit M1); n_trials_from_weight_schemes stays 8 so the degrade path is intact. NOTHING ELSE MOVED: leaf-by-leaf diff gives 11 moved / 0 ADDED / 0 REMOVED - five ARE the DSR chain, four are provenance (generated_at, generated_at_utc, git.commit, git.short) and the remaining two moved by 0.000% last-digit float (costs.top_decile.realised_one_way_bps and one no_trade_band turnover). long_short_tstat 2.8360640685320595, top_decile_alpha 0.07174142332098163, monotonicity -0.8909090909090909 and universe 2531/69 are bit-identical. Zero additions this time, unlike session 13's 32, which is the expected signature of a refresh one day rather than five sessions behind. Both shipped decisions unchanged (low_risk confirmed_oos, insider rejected_oos), cpcv.adopt still false so the shipped strategy takes no haircut, and the sanity layer fires its two expected flags with neither silenced. No trial was run by the re-run itself, so it adds nothing to N. |
| PT-AMEND1 | 2026-08-09 | equity | yes | Same hypothesis as PT-REGISTER - the Valquo Index beats SPY on a forward out-of-sample track - but re-registered as RUN #2 after run #1 was VOIDED, and governed from here by a VINTAGE RULE | PAPER_TRACK_CONTRACT.md section 5a, in force from this commit. Run #1 (inception 2026-07-30, 6 days, 2 recorded rows) is VOIDED under section 3's existing clause 'any change to how the Index is constructed' - the growth-input fix, score fix and universe rebuild mean the measured model no longer exists. Run #2 opens at inception 2026-08-10 with ZERO accrued days; operational gate 2027-02-10; verdict 2031-08-10. VINTAGE RULE: any ADOPTED change to scoring, weights or construction closes the current vintage and opens the next; rebalancing under unchanged rules is NOT a vintage event; each vintage carries its own clock; the gate and the meter attach to the CURRENT vintage; the cross-vintage chain is kept and published as 'the system as operated' and is explicitly NOT the object of a verdict because it mixes models | REGISTERED | n=1 | HANDOFF_edge_audit.md session 15; PAPER_TRACK_CONTRACT.md section 5a; valuation/edge/track_meter.py VINTAGES; tests/test_track_meter.py (29 tests). CHARGED, NOT WAIVED, AND THIS IS THE POINT OF THE ROW: void-and-restart is a researcher degree of freedom - each vintage is another chance for the same hypothesis, so the probability that SOME vintage crosses rises with the number of vintages. Section 5a rule 6 is the brake (a vintage change resets the whole accrued clock and buys nothing statistically); this charge is the accounting. Equity N 130 -> 131. THE AMENDMENT MOVED THE CLOCK, NOT THE STATISTICS: sigma 3.9846917305386294, rho 3, alpha 0.05, cost drag 0.14529 and the SUPPORTED/UNSUPPORTED bars are all unchanged, pinned by test_the_amendment_moved_the_clock_and_not_the_statistics. Sigma was RE-CHECKED against the changed model rather than assumed to survive it: the current backtest still gives SPY excess +9.99 pct/yr at an implied tracking error of 11.401 pp/yr and IR 0.8759/yr, the figures sigma was derived from. DISCLOSED BECAUSE IT IS THE OBJECTION: the voided window was KNOWN to be -2.85pp when it was voided, so discarding it is the flattering direction. Three things answer that and each is checkable - the cause (model change) is independent of the outcome and the clause pre-existed the run; run #2 accrues ZERO days before its inception so no window's sign could inform the new start date; and the voided rows are KEPT, appearing in as_operated() where anyone can see what was excluded. Voiding a vintage for a change CHOSEN after seeing the vintage go badly is forbidden by 5a and is the distinction the rule makes mechanical. |
| ARTIFACT-N15 | 2026-08-10 | infra | n/a | Charging PT-AMEND1 moves equity N 130 -> 131, so the canonical BACKTEST_RESULTS.json again ships a Deflated Sharpe at the previous, flatteringly smaller denominator | the artifact's deflated_sharpe must equal research_log's equity N at the current count, with every other published figure unchanged | FIXED | n=1 | HANDOFF_edge_audit.md session 15. Full re-run on the 2,531-name / 69-date universe from a CLEAN tree at 68aba51, git.dirty false. DSR 0.8547321268980206 at n_trials 130 -> 0.8538605963614212 at 131; sr0_benchmark 0.43075197686098127 -> 0.4311821065572387; n_trials_source still RESEARCH_LOG.md (audit M1); n_trials_from_weight_schemes stays 8 so the degrade path is intact. NOTHING ELSE MOVED: leaf-by-leaf diff gives 14 moved / 0 ADDED / 0 REMOVED - five ARE the DSR chain, four are provenance, and five moved by 0.000% last-digit float (three costs fields and two no_trade_band curve entries). long_short_tstat 2.8360640685320595, top_decile_alpha 0.07174142332098163, monotonicity -0.8909090909090909 and universe 2531/69 are bit-identical. Both shipped decisions unchanged (low_risk confirmed_oos, insider rejected_oos), cpcv.adopt still false so the shipped strategy takes no haircut, sanity layer fires its two expected flags with neither silenced. THE FIRST ATTEMPT WAS KILLED MID-LOAD and wrote nothing, which is why the artifact was briefly one trial stale rather than wrong - it was re-run rather than patched. No trial was run by the re-run itself, so it adds nothing to N. |
| V5 | 2026-08-09 | infra | yes | Realized paper fills can be compared against the modelled options cost, so the cost model every options number is net of becomes testable forward rather than assumed | scripts/slippage_report.py measures the exit half-spread paid against a modelled 410.0 bps mean, 90% CI from a percentile bootstrap over CALENDAR-WEEK blocks (2,000 draws, seed 0), and quotes NO aggregate below 30 filled legs; DIVERGENT-COSTLIER if the CI lies above the bar, DIVERGENT-CHEAPER if below, CONSISTENT if it straddles, INSUFFICIENT under n=30 | ADOPTED | n=1 | HANDOFF_optionsbot.md 2026-08-09; PREREG_v5_slippage.md committed at c06ac55 BEFORE the script existed; VALQUO_EXTENSIONS.md V5; data/options_slippage/V5_SLIPPAGE_2026-08-09.json; tests/test_slippage_report.py (52 tests). THE HEADLINE IS INSUFFICIENT AND THAT IS THE PRE-REGISTERED OUTCOME: the paper book holds 3 entry fills and ZERO exits, so the exit half-spread has n=0. The pre-registered expectation was INSUFFICIENT at 90/10 and it was RIGHT. THE MODELLED BAR IS MEASURED, NOT ASSUMED: entry half-spread mean 410.0 bps of premium / median 333.3 on 3,885 of 3,885 banked R2-corrected trades (entry_spread_pct / 2), against a $2.58 median premium and $1.30 round-trip commission = 50.4 bps. THE BRIEF'S "modelled 33.4bps" IS AUDIT B11'S EQUITY COST IN BPS OF STOCK NOTIONAL and applying it to a premium-denominated options book is a category error of about 12x; recorded rather than silently swapped. WHAT THE THREE REAL FILLS ALREADY SHOW, reported as n=3 raw values with no aggregate: entry fill vs its own limit is -2022.5, -612.2 and 0.0 bps. CORRECTED WITHIN THE SAME SESSION BEFORE LANDING: this row first read that the sandbox granted up to 20.2% PRICE IMPROVEMENT, crediting the fill engine. THE TIMESTAMPS REFUTE THAT. Every order waits 12.8 to 15.9 hours between the limit being placed and the fill, and all three fill at 13:46-13:47 UTC = 09:46-09:47 ET, the opening minutes, because auto-scan.yml schedules the paper cycle at 20:47/21:47 UTC = 4:47pm ET, AFTER THE CLOSE. The limit is therefore set from a POST-CLOSE quote and the day order fills at the NEXT OPEN, so fill-vs-limit on this book is an OVERNIGHT GAP and not execution quality. Two consequences, both real: the paper book's entry basis is not the backtest's (TGT's alert-day ask was 4.55 and the paper book paid 3.55, 22% better for a reason the backtest does not model, which flatters the forward track on entry), and it makes defect (1) below SYSTEMATIC rather than occasional. The instrument now ships diagnostic_submit_to_fill so the elapsed time prints beside the number. TWO SHIPPED-CODE DEFECTS FOUND IN THOSE SAME THREE ROWS, both reported not repaired because V5 is scoped NEW FILES ONLY: (1) paper_track._place_entry derives target_premium and stop_premium from the SUBMIT price and mark_open never recomputes them to the actual fill, so TGT is running +150.7% / -37.3% and MET +113.0% / -46.7% against an intended +100% / -50% - 2 of 3 live positions are running a strategy no backtest describes, same family as audit B5c which fixed only the resume branch; (2) submit_new_alerts never reads the alert's own features.sizing, so ETN was bought despite the alert carrying skip=true, contracts=0, "one contract costs $1,610, above the $1,000 budget" - and it is the largest position in the book. ALSO ROUTED: the ENTRY half-spread is not measurable at all because paper_option_orders stores no bid/ask/mid at submit; the submit ASK is recoverable from target_premium (corroborated independently by the stop column, which agrees to 4dp on all three rows) but a half-spread needs a MID. Fix is two columns in _place_entry. The instrument reads the git-committed Render backup (data_export/paper_track_history.json) because every screener store off Render holds zero paper rows - the backup written to protect the record doubles as the read path for measuring it. Instrumentation searches nothing, so this is charged to infra on the HACFLOOR / CHAINFREEZE precedent: options N stays 192 and equity N stays 130. |
| PT-BUG12 | 2026-08-10 | infra | n/a | The paper options book's live exit levels do not describe the strategy being tracked: _place_entry anchors target_premium and stop_premium to the price the order was SUBMITTED at, and mark_open then overwrites entry_premium with the broker's actual fill without recomputing either level. Separately, _eligible never reads the alert's own features.sizing, so the book buys names the live product refused | every live row must satisfy target/entry == 1 + target_pct and stop/entry == 1 + stop_pct on the ALERT's own logged policy, to six decimals; the repair must be idempotent, must touch only the two level columns, must leave a row whose fill equalled its limit bit-identical, and must NOT execute a trade if a corrected level is already crossed. Expected values pre-committed per row before any code changed, in PREREG_session16_paper_track_repair.md sections 2 and 3 | FIXED | n=1 | HANDOFF_edge_audit.md session 16; routed by HANDOFF_optionsbot.md sections 4 and 9 off the first three REAL fills; tests/test_paper_track.py (70 tests). ALL FIVE PRE-COMMITTED CRITERIA HELD EXACTLY, verified by replaying the committed Render export data_export/paper_track_history.json into a temp store: TGT target 8.90 -> 7.1000 and stop 2.225 -> 1.7750; MET 9.80 -> 9.2000 and 2.450 -> 2.3000; ETN 32.20 and 8.050 BIT-IDENTICAL because its fill equalled its limit. Exactly 2 of 3 rows changed, all three end at target/entry 2.000000 and stop/entry 0.500000, the second pass repairs nothing, and no field outside the two levels plus note and updated_at moved. THE DIAGNOSIS WAS CORROBORATED BEFORE THE FIX: dividing each live target by 2 and each live stop by 0.5 recovers the SAME three submit prices (4.45, 4.90, 16.10) - two different multipliers agreeing to four decimals. THE REPAIR RUNS IN THE FLATTERING DIRECTION AND THAT IS DISCLOSED: both changed rows move their target DOWN (easier) and their stop DOWN (looser), so the bug ran against the paper book and the fix runs for it. MET sat 10.2 pct above a stop level no backtest specified and now sits 17.4 pct above its specified one. BUG 2: applied to the three logged alerts, exactly 1 is refused and it is ETN, whose own sizing carried skip=true, contracts=0, 'one contract costs $1,610, above the $1,000 budget' - and it is the largest position in the book. The veto is honoured; the sizing QUANTITY is deliberately NOT read, because that would change the book's construction, and a test pins the non-change. The already-open ETN position is NOT unwound - _eligible gates new entries only, and closing a live position to tidy the record is a trade decision. A THIRD DEFECT WAS FOUND WHILE FIXING THE FIRST, same family, and is fixed here: the resume branch called _exit_policy on a paper_option_orders row, and that table has NO features column, so the policy silently collapsed to the DEFAULTS. Audit B5c's own comment claims that branch 'rebuilds the exit policy the same way the fresh path does' - the fresh path reads the ALERT. It has never fired because all three live alerts happen to use the default policy, which is luck rather than protection, so it is pinned with a policy that differs. A read-only guard (options_summary.level_conformance) now reports off-spec rows on every request; the first time this book was inspected, 2 of 3 open positions were off spec and nothing anywhere said so. Correctness repair, searches nothing: equity N stays 131. |
| PT-SPLIT | 2026-08-10 | infra | n/a | Two mechanisms record something called the Valquo Index and they hold DIFFERENT books, so a figure from one can be, and once was, published as the other | a book is the contract-bound Valquo Index if and only if it holds at least CONTRACT_MIN_POSITIONS = 50 names AND its 8 pct cap actually binds (effective_max_weight <= MAX_WEIGHT). Verdict on the live engine book pre-committed as NON-CONFORMING before measuring, in PREREG_session16_paper_track_repair.md section 4. Either the engine is aligned to the contract's construction, or the recorded days are registered as a separate experiment that may never be quoted as the Index. No third state | FIXED | n=1 | HANDOFF_edge_audit.md session 16; PAPER_TRACK_CONTRACT.md section 5b; tests/test_paper_track.py. THE ROUTED DIAGNOSIS WAS WRONG AND THE CORRECTION IS THE USEFUL PART. The split was reported as the engine violating the 8 pct cap by holding 10 names at 10 pct each. It is not a cap violation: valquo_index.build_index sets cap = max(MAX_WEIGHT, 1/len(picks)) with an explicit comment saying why - ten names at 8 pct sum to 80 pct - and the payload has always self-reported effective_max_weight. The weights were correct for the book they described. THE REAL DIVERGENCE IS BOOK SIZE, 10 names against the published Index's 86, and it is ONE construction fed TWO inputs: n = max(MIN_NAMES, round(len(large) * TOP_DECILE)) with MIN_NAMES = 10, so a 10-name book means the eligible large-cap tier held FEWER THAN 100 names while the published 86 implies a tier of roughly 860. /admin/run-paper-track reads data/valquo_index.json when it exists and SILENTLY REBUILDS FROM THE STORE'S LATEST SCAN when it does not, and that scan is a top-N hot list. The engine was never pointed at a different strategy; it was handed a truncated one with no label saying so. That is how PT-OUTBOUND happened - a Discord recap read the engine and published +0.18pp while the bound recorder read -2.85pp. ENFORCED: seed_book now REFUSES a non-conforming book and says why, loudly and non-destructively (the run continues and marks what is already held, because silently liquidating a live sandbox book on a conformance rule would be worse than the split). The only other way through is experiment=True, which STAMPS EVERY HOLDING ROW - on the row rather than in a return value, because PT-OUTBOUND's lesson is that the old code DID label its fallback honestly and no surface ever rendered the label. index_summary carries book_conformance measured FROM THE HOLDINGS rather than remembered from a seed run. The four recorded days (2026-08-03, 04, 05, 07; 10 names at 0.10) are REGISTERED AS A SEPARATE EXPERIMENT and KEPT, not deleted - deleting a record because it turned out to describe the wrong book is the flattering direction. Its FILLS remain real evidence about execution and are what V5 measures; its RETURN SERIES is evidence about nothing the contract binds. This does NOT open or close a vintage: the engine's series was never the bound series, so run #2's inception 2026-08-10, gate 2027-02-10 and verdict 2031-08-10 are untouched. STILL OPEN: the alignment is a GATE, not a repair - nothing here makes the engine start recording the real 86-name book, which needs a conforming data/valquo_index.json present on the Render disk when the cycle runs. Until then the gate's effect is that the engine stops ADDING to a non-conforming book. Equity N stays 131. |
| V1 | 2026-08-10 | infra | yes | An ADOPTED scoring change can be given forward evidence without waiting out a fresh five-year vintage, by scoring the previous vintage's frozen composite in shadow on the same dates and comparing the two books as a PAIRED difference - because both books see the same market, so the market risk that dominates a vs-SPY test cancels | PREREG_v1_shadow_vintages.md, committed before any vintage pair exists. Paired monthly difference d = live vintage excess minus shadow vintage excess, positive meaning the adoption helped. Robbins normal-mixture anytime-valid boundary, rho 3, alpha 0.05 two-sided, sigma = (annualised TE between the two books) / sqrt(12) * sqrt(1.466091), the AR(1) design effect, measured ONCE at pair open and never revised downward. NOT-COMPARABLE if weight overlap < 0.20 (checked first); INSUFFICIENT below 6 complete months; CONFIRMED-LIVE if cumulative difference exceeds the boundary; HARMED if it exceeds it downward; NULL otherwise, always quoted with the difference it could have detected | REGISTERED | n=1 | HANDOFF_edge_audit.md session 16; VALQUO_EXTENSIONS.md V1; valuation/edge/shadow_vintage.py; tests/test_shadow_vintage.py (26 tests, every frozen parameter pinned to a literal). NO MEASUREMENT AND NO VERDICT - this row is logged at REGISTRATION. GENUINELY BLIND, and more so than any previous register in this project: vintage 2 opened 2026-08-10 and has NO successor, so no vintage pair exists at all and not one parameter could have been chosen to suit a comparison, even in principle. Vintage 2's parameters are pinned NOW, in a tracked file, at params_id 0060c5ef3dda, so the shadow will run a pinned snapshot rather than a reconstruction. THE POWER IS STATED IN ADVANCE AND THE HONEST HALF IS UNFLATTERING: pairing is a large real gain - at an annualised between-book TE of 2.0 pp/yr the 60-month detectable difference is 3.34 pp/yr against the vs-SPY meter's 19.01 - but the tension is STRUCTURAL and no design escapes it. Sigma is small exactly when the two books overlap, i.e. when the adoption changed little; an adoption big enough to matter also raises sigma. So a shadow pair that has not crossed is the EXPECTED outcome and is NOT evidence that the adoption was worthless, and verdict() carries that sentence in its own output rather than leaving it in a markdown file. THE CONTROL IS EXACT: fed the vs-SPY meter's own 11.40 pp/yr tracking error this machinery reproduces the contract's published sigma 3.9847 and its published ~19 pp/yr at 60 months to the digit, because it IMPORTS track_meter.boundary rather than re-implementing it - one boundary function in the project, so the guarantee cannot change for one meter and not the other. Rho and alpha are imported, not re-declared, and a test fails if they are copied. The verdict has NO SIGN BRANCH - an AST test requires exactly one, the one that names the crossing direction - and a test flips the sign of an entire series and requires the verdict to flip with it and nothing else to move, so HARMED is exactly as reachable as CONFIRMED-LIVE. SCOPE, fenced before it has any numbers to leak: research instrumentation only, never any public surface, pinned by a walk over valuation/saas and valuation/web plus the templates - PT-OUTBOUND is why this fence exists in advance rather than afterwards. It does not touch the contract, cannot pass the operational gate, and does not weaken Amendment 1 rule 6: an adoption still resets the vintage clock. This measures the price; it does not refund it. A registered instrument that has produced no measurement searches nothing, so it is charged to infra at n=1 on the HACFLOOR / CHAINFREEZE precedent: equity N stays 131. The FIRST SHADOW PAIR is a trial and is charged when it opens, not now. |
| V2G | 2026-08-10 | equity | yes | The three themes that reach no live score (insider CONSTANT, capital_discipline and institutional ABSENT on 100% of served rows, per HANDOFF_live_data_bugs.md Part 12.7) carry 0.375 of the 0.875 weight mass, so the live hot list is a FOUR-theme book wearing the weights of a seven-theme one. Does that restriction cost return, and does the four-theme book the users actually receive still stand on its own | PREREG_v2g_live_theme_cost.md, committed alone at 6d8750a BEFORE scripts/live_theme_cost.py existed and before any arm was scored. Two pre-specified arms and NO selection step: A7 = deployed value quality momentum insider capital_discipline size institutional at 0.125 each; B4 = value quality momentum size at 0.125 each. Primary statistic is the PAIRED per-period difference of top-decile alpha, HAC lag 1, because both arms are scored on the same dates and differencing cancels the market move that dominates each level. MATERIAL if d_alpha <= -1.95pp (X7's calibrated top-decile alpha margin) AND paired HAC abs(t) >= 2.0; IMMATERIAL if d_alpha > -1.95pp AND paired HAC abs(t) < 2.0; anything else is a NULL. Reported separately: does B4 clear the calibrated absolute floors of 2.2837 (long-short HAC t) and 2.2913 (alpha HAC t) | IMMATERIAL ON COST, BUT THE LIVE BOOK FAILS THE CALIBRATED LONG-SHORT FLOOR | n=4 | HANDOFF_edge_audit.md session 17; data/free_analysis/LIVE_THEME_COST.json; scripts/live_theme_cost.py; tests/test_edge.py (4 new V2G tests). Corrected 2,531-name / 69-date panel, 113,945 rows, 2009-01-15 to 2026-01-28. THE INCUMBENT ARM REPRODUCES THE RECORD TO THE DIGIT, which is the harness control: A7 gives top_decile_alpha 0.071741423321 (+7.17%), long-short naive t 2.8361, HAC t 2.6199, alpha HAC t 4.3762, monotonicity -0.891 - every one matching the figures CLAUDE.md already carries. RESULT: B4 alpha +5.86% against A7's +7.17%, so d_alpha = -1.3133pp against the -1.95pp bar, and the paired HAC t is -1.4040 over 69 paired dates. Both conditions of the IMMATERIAL branch are met, so the pre-registered verdict is IMMATERIAL and the cost of the three dead themes is NOT separable from zero. THE POWER CAVEAT MUST TRAVEL WITH THAT VERDICT: the HAC standard error of the annualised paired difference is 0.9354pp, so the smallest gap this design can detect at abs(t)=2 is 1.8708pp - well matched to the 1.95pp bar it committed to, but the power to detect a TRUE 1.95pp gap is only 55.0%. IMMATERIAL here means the cost could not be separated from zero at a coin-flip's power against its own bar, NOT that the cost was shown to be small. THE SECOND FINDING IS THE MORE SERIOUS ONE AND IT IS WHY THE REGISTER ASKED FOR IT SEPARATELY: the live four-theme book does NOT clear the calibrated long-short floor. B4's long-short HAC t is 1.8811 against the 2.2837 floor and its naive t is 2.0044 against 2.1437 - it fails both - where the deployed seven-theme book clears at 2.6199. B4 DOES clear the top-decile alpha HAC floor, 3.2087 against 2.2913, and since the shipped product is a LONG-ONLY hot list that is the product-relevant statistic. So the long-only book users receive remains demonstrable against a calibrated bar; the long-short research statistic it is quoted alongside is not. The paired long-short difference is -3.00pp/yr at HAC t -1.8792, closer to significance than the alpha difference. BOTH SPLIT DIRECTIONS AGREE IN SIGN AND NEITHER IS SIGNIFICANT: early half d_alpha -1.14pp (paired HAC t -1.3519, 34 dates), late half -1.48pp (t -0.8984, 35 dates). Unlike session 7's LOO arms, four of seven of which changed sign between halves, this delta is stable in direction. CONTROLS. C1 and C2 are EXACT at 0.000e+00 over all 113,945 rows: scoring the SEVEN-theme weight vector on a panel where the three dead themes are NaN (C1), and on one where insider is CONSTANT and the other two absent (C2, the live condition exactly), both reproduce the four-theme arm name for name. That is what makes B4 the live book rather than a model of it, and it holds because composite renormalises by the present-weight mass and zscore returns all-NaN on a zero-variance column. C3: the calibrated floors were measured at N=121 and re-verified at N=129, and adoption is monotone decreasing in N, so an identical adopter set means identical scores and identical percentiles - recomputed from the banked X7_RECONCILE margins, 20 adopters at N=129 and 20 at N=135, set identical, so the floors still stand at this N. The two arms scored the SAME 69 dates and the same median 1,557 names, so the register's stated risk that B4 could rank fewer names did not materialise. EXPLORATORY DECOMPOSITION, NO VERDICT, and pre-registered as carrying none because session 7 established on this exact panel that a full-sample ablation arm is not a finding: dropping insider alone gives +0.30% on the full sample but FLIPS SIGN across halves (-1.39% early, +1.94% late), consistent with the record's own note that insider's t is not a measurable quantity; dropping capital_discipline gives +1.37% full and is POSITIVE in both halves (+0.20% early, +2.51% late), i.e. the theme with the second-strongest panel IC (+2.76) and one of only two clearing X7's 2.71 bar appears to cost nothing to lose - a direct restatement of X3's finding that theme IC does not predict marginal contribution, and its early-half +0.20% coincides with the value session 7's held-out LOO measured for the same arm; dropping institutional is the ONLY one negative in both halves (-1.41% full, -0.89% early, -1.91% late). If one live source is built, 13F is the one the evidence points at, and that sentence is a build-priority hint, not a result. WHAT THIS DOES NOT SAY, and it is the part most likely to be misquoted: an immaterial ALPHA cost is not a finding that the live absence is acceptable. The live product computes a different composite from the one every published figure is measured on, which is a claims-integrity issue independent of return - audit B7 exists because live and backtest scoring diverged before. Equity N 131 -> 135, charged as four arms (B4 plus the three decomposition arms); the halves are the same arms on subsets rather than new hypotheses and are not charged, on session 7's precedent of charging 7 for 7 LOO arms measured in both directions. THE PRE-REGISTERED EXPECTATION WAS WRONG AGAIN: MATERIAL was predicted at 55/45 and the result is IMMATERIAL; B4 clearing the long-short floor was predicted at 50/50 and it fails; institutional being the costliest of the three was predicted at 55/45 and it is the only consistently costly one; insider being neutral-or-helpful was predicted at 70/30 and holds; at least one decomposition arm flipping sign between halves was predicted at 65/35 and exactly one does. |
| ARTIFACT-N17 | 2026-08-10 | infra | n/a | Charging V2G's four arms moves equity N 131 -> 135, so the canonical BACKTEST_RESULTS.json committed at 8ad6c0f ships a Deflated Sharpe computed at the previous, flatteringly smaller denominator - the same silent staleness sessions 13, 14 and 15 each had to repair, re-created by this session's own row | the artifact's deflated_sharpe must equal research_log's equity N at the current count, with every other published figure unchanged | FIXED | n=1 | HANDOFF_edge_audit.md session 17. Full re-run on the 2,531-name / 69-date universe from a CLEAN tree at 8ad6c0f, git.dirty false. DSR 0.8538605963614212 at n_trials 131 -> 0.8504129179654681 at 135; sr0_benchmark 0.4311821065572387 -> 0.43286698556205233; metric still self-reports deflated_sharpe_ratio; sqrt(2 ln 135) = 3.1322, still above the Harvey-Liu-Zhu hurdle of 3.0. NOTHING ELSE MOVED: leaf-by-leaf diff gives 16 moved / 0 ADDED / 0 REMOVED - five ARE the DSR chain, four are provenance (generated_at, generated_at_utc, git.commit, git.short) and the remaining seven moved by 0.000% last-digit float (three costs realised/breakeven fields and four cost-curve entries). long_short_tstat 2.8360640685320595, top_decile_alpha 0.07174142332098163, monotonicity -0.8909090909090909 and universe 2531/69 label full are all BIT-IDENTICAL, and errors is empty so no validation block degraded. Both shipped decisions unchanged (low_risk confirmed_oos, insider rejected_oos) and cpcv.adopt is still false, so the shipped strategy takes no haircut. THE FIRST ATTEMPT DIED SILENTLY AT 60 OF 69 REBALANCE DATES with no traceback and roughly 5.9 GB resident, writing nothing, which is why the artifact was briefly one register stale rather than wrong - it was RE-RUN, not patched, even though the Deflated Sharpe is a closed-form function of N and ablation.deflated_sharpe_at would have computed it directly. Patching the artifact by hand is the one repair this file's own history forbids. Reported as a defect in its own right: a full run that dies at 87% with no error and no partial output is indistinguishable from one that never started, and the second attempt survived the same point at 6.05 GB, so it is memory pressure rather than a deterministic fault. No trial was run by the re-run itself, so it adds nothing to N. |
| LA15 | 2026-08-10 | infra | n/a | The test suite writes production-shaped state into the repository's REAL databases, so a local run leaves a year-2099 snapshot that outranks every real scan on every scan-derived surface | every suite must leave data/ byte-identical, measured by fingerprinting size, mtime and sha1 of data/, data_export/, every repo-root file and git status around a subprocess run of each suite, before and after | FIXED | n=1 | VALQUO_LIVE_AUDIT.md LA15; HANDOFF_optionsbot.md session 19; tests/state_isolation.py; tests/test_state_isolation.py (29 tests). THE AUDIT NAMED ONE ROW AND THE MEASURED BLAST RADIUS IS SIX, ACROSS FIVE TABLES: one run of tests/test_saas.py leaves meta hot_processed_2099-01-01, a 2099-01-01 scan, a TESTX snapshot row, a hot10 track_pick, an OPEN hot10 paper position in TESTX at 10.00 dollars, and an alerts_sent row for __HOTDIGEST__ stamped with the REAL calendar day - and that last one is not cosmetic, because notify.post_hot_digest returns early when alerted_today fires, so running the suite on a box with a Discord webhook SUPPRESSES that day's real hot digest. THE SWEEP FOUND THREE LEAKS THE AUDIT DID NOT NAME: test_security.py writes data/app.db, test_screener.py writes data/archive/scans/TODAY.json.gz, test_private.py opens the real screener.db; three more suites READ the real accounts DB through create_saas_app and test_paper_track.py reads data/valquo_track.json. FOUR ESCAPES, ENUMERATED NOT INFERRED: create_saas_app binds UserStore(cfg.database_url); a bare Store() or UserStore(); run_scan with save=True, where screen.py archives to the RELATIVE default root data/archive, invisible from a call site that had correctly passed a temp store; and live_hero, through index_track.default_paths(). THE CROSS-LANE FINDING: all 5 archived scan days here and all 3 in the primary checkout read provider synthetic (offline test) with 100 SYN-prefixed tickers, one file per calendar day the suite was run, and scripts/theme_health.py (V2) reads exactly that directory - so V2's NOT-QUOTABLE verdict on every archived scan day being synthetic has a named cause, and the directory is SELF-REFRESHING, accruing one more plausible fabricated day per day. THE SECOND FINDING: with the primary checkout's real track files present the PRE-FIX test_paper_track.py scores 65/70 and the fixed suite 70/70, and two of the five failures assert the opposite of what happened - test_hero_will_not_render_the_sandbox_book_as_the_index reports that the hero rendered the Tradier sandbox book while the payload it prints reads source index-track and book Valquo Index, i.e. the CONTRACT-BOUND recorder read correctly, so those local failures must never be quoted as a B7 sandbox leak. The guards are not weakened: CI has no track files, so an empty temp directory reproduces CI behaviour exactly. VERIFIED BY RE-RUN: the identical sweep after the fix reports every suite rc=0 and an EMPTY mutation list, 4 mutating suites before and 0 after. NOTHING WAS DELETED - the primary checkout's pollution is Don's call and the targeted statements are in the handoff. SCOPE tests/** only, so archive.DEFAULT_ROOT staying relative is reported and routed to the screener lane, not repaired here. A correctness repair searches nothing, so this is a FIXED row and N does not move: equity stays 135, options 192, infra 6, total 333, rows_malformed empty before and after. |
| PATHSTUDY | 2026-08-10 | options | yes | The shipped options exit (+100% target, -50% stop, half-DTE time stop) was only ever raced against STATIC ladders (O1) and against a P&L decomposition (O23). Path-CONDITIONAL rules were never tested, and the paths themselves were never described: how deep a trade digs before it dies, whether a -40% option comes back, how long a winner takes, and what a winner does after it doubles | PREREG_path_study.md, committed at 9d37241 BEFORE any table existed and containing the whole stage-2 arm set, so no arm could be chosen to suit stage 1. Stage 1 is descriptive with no verdict and zero trials. Stage 2 is 13 arms across six pre-registered families, each diffed against O1's 21-policy grid so nothing rejected re-runs, scored by paired per-trade expectancy against the shipped policy on identical entries, selected on one half of the book and measured on the other in BOTH directions, with month-block clustered CIs and the same five pooled random-entry control seeds R2 used | REJECTED - no arm clears the bar, and the exit's whole effect turns out to be independent of the entry | n=13 | HANDOFF_optionsbot.md session 20; scripts/path_study.py, scripts/path_arms.py, scripts/path_gate.py; tests/test_path_study.py (22 tests); data/options_pathstudy/PATHSTUDY_STAGE1.json and PATHSTUDY_STAGE2.json. THE INSTRUMENT REPRODUCES THE RECORD TWICE, WHICH IS WHAT MAKES THE REST QUOTABLE: paths were rebuilt for 3,885 of 3,885 banked trades and 29,783 of 29,785 control trades from the frozen chains, because data/options_exitlab/paths.pkl covers only 1,099 of the 3,885 banked trades (28.3%) and carries 2,020 the book does not - a different trade set. Replaying the shipped policy on the rebuild reproduces the banked book on 3,885/3,885 exit reasons AND 3,885/3,885 P&Ls to within 1e-9 (mean abs error 0.0) at +3.41%/trade, and the control at +10.06%/trade - both R2's recorded headlines to the digit. O1's own note records 86.950% for the same replay; that was under the PRE-B2 quote rule its docstring was diagnosing, so this corroborates the diagnosis. The entry bid is not stored by the book and is recovered as bid = ask*(2-s)/(2+s), max abs error 0.000000000 against the 1,099 trades that carry a true entry_bid. STAGE 1, the descriptive answer: 80.2% of trades touch -50% at some point and the median drawdown before the banked exit is -51.5%; even trades that ended at TARGET had a median -10.0% drawdown first. A -50% touch cuts the chance of ever reaching +100% from 44.7% to 17.5% and only 35.4% see breakeven again, so the inherited stop sits at an informative level. THE STRONGEST STRUCTURE IS TIME, NOT LEVEL: from a -50% touch, recovery to breakeven runs 9.2% with 7 or fewer days left (n=153), 30.7% at 8-21, 34.2% at 22-45 and 41.6% beyond 45 days (n=1,187) - four and a half times. Winners are fast: median 20 days to +100%, one third of the original DTE, and 67.6% of targets arrive with more than half the DTE unused, so the time stop is not cutting winners short. POST-TARGET IS A BARBELL AND IT VINDICATES CLOSING AT THE DOUBLE: of 1,174 trades reaching +100% with more than half their DTE left, 67.5% go on to +200% but 83.2% fall back below +100% and 58.0% eventually go below zero, median final +62.2% against a p25 of -94.4%; only 6.9% lose their last quote more than 5 days early, so this is not a stale-mark artefact. STAGE 2: the largest gain anywhere is trail50_after100 at +3.60pp against O1's own pre-committed MIN_EXPECTANCY_GAIN of 10pp, imported rather than restated so it cannot be quietly lowered. Both split directions select THAT SAME ARM - more stability than session 7's LOO managed - and neither measured half clears the bar (+5.77pp and +1.42pp) with both clustered CIs straddling zero. THE RESULT THAT OUTLIVES THE VERDICT: scored on the five pooled random seeds, every arm does almost exactly the same thing to a book of RANDOM entries as to the alert's book - Pearson r 0.967, regression slope 0.990, 13 of 13 same sign, mean absolute difference 0.34pp and a maximum of 0.86pp. O23 found HALF of an exit's P&L difference is the underlying; on this arm set essentially ALL of it is independent of the entry, so an exit rule is a property of options rather than of the signal. Under O1's gate that is the supportive branch (X1 requires an arm to help both entry sets, to catch book-specific fits) - these arms are not fitted, they are just too small. THE PRE-REGISTERED EXPECTATION WAS RIGHT FOR ONCE: REJECT was predicted at 70/30, and stage 1 showing structure at 75/25. The nominated survivor time_cond25 came in at +0.12pp. THE UNCOMFORTABLE ONE: sl_by_dte, the arm stage 1 appears to motivate, is the WORST in the study at -2.14pp with a CI excluding zero - its conventional parameterisation widens the stop near expiry, which is where stage 1 says the trade is already dead. The flipped version is a NEW pre-registration and was deliberately NOT run, because running it now would be selecting the rule on the result. ALSO FOUND: families C and D are nearly inert - stock_stop fires on 0.4% of trades, ivcrush30 on 1.3%, delta85 on 2.4% - because by the time the underlying is -8% the option is already through its -50% stop, so the option-level stop dominates every underlying- or state-level trigger tested; and be50 and step50 are literally the same arm under a +100% target, since the first +50% ratchet step lands exactly at breakeven. NOTHING SHIPS, to either book, and none of this reopens R2: the entry is still dead and no exit rule can rescue it. Options N 192 -> 205; equity N untouched at 135. |
| S22 | 2026-08-10 | equity | yes | Every HEADLINE figure this project publishes is measured at a 63-day forward window because build_fundamental_panel computes exactly one fwd_ret and the deployed rebalance period equals it, which is an inherited default and not a measured optimum. CORRECTION TO THIS REGISTER'S OWN PREMISE, recorded rather than edited away: the register claimed nobody had ever asked what the composite predicts at longer horizons and that is FALSE - BACKTEST_RESULTS.json has always carried a per_horizon block at 63/252/756, which however reports IC and weight vectors ONLY and sets rb = max(rebalance_days, H) so the rebalance period moves with the horizon, giving 69 dates at 63d against 18 at 252d and 6 at 756d on universes of 2,531 / 2,492 / 2,206 names - the exact confound this design removes, and six dates support no inference. It does CORROBORATE the result: that block's out-of-sample IC rises monotonically with horizon, 0.038990 to 0.058241 to 0.097671, direction only since the IC definitions and date sets differ. Does the composite's edge decay, persist or reverse as the forward window lengthens to two years, and how long does a name survive in the top decile that IS the product | PREREG_s22_term_structure.md, committed ALONE at 6b187dd BEFORE scripts/term_structure.py existed and a strict git ancestor of the measurement commit ec4a5d3. Eight pre-specified horizons of 63 126 189 252 315 378 441 504 trading days, i.e. 1 through 8 quarters, all scored from ONE panel build carrying eight fwd_ret_h columns computed in the same loop from the same price array, because the panel's grid end is len(cal) minus horizon and a build per horizon would vary the horizon AND the date set AND the cross-sections together. Primary statistic is CUMULATIVE non-annualized top-decile alpha, because annualizing divides by the horizon and would make a fixed one-off edge look like it decays as 1/k. Primary date set is the COMMON 62 dates observable at every horizon so the horizon is the sole varying quantity. HAC lag fixed by the design at max(1, H/63 - 1), the overlap the grid induces, which is the shipped R9 lag 1 at h63. CONSTANT-RATE if R(8) at least 6.0, SATURATING if at most 2.0, INTERMEDIATE between, REVERSING overrides if cumulative alpha turns negative at any horizon of at least 189d with alpha HAC t at most -2.0 | CONSTANT-RATE - THE EDGE DOES NOT DECAY OVER TWO YEARS, BUT THE LONG-SHORT SPREAD DOES | n=8 | HANDOFF_edge_audit.md session 18; data/free_analysis/TERM_STRUCTURE.json; scripts/term_structure.py; tests/test_edge.py (11 new S22 tests). Corrected 2,531-name / 69-date panel, 113,945 rows, 2009-01-15 to 2026-01-28, common window 2009-01-15 to 2024-04-24. CONTROLS ALL PASS. C0, the added column IS the shipped column, max abs deviation 0.000e+00 over all 113,945 rows. C1, the incumbent arm reproduces the published record to the digit on a FRESH build rather than a cached pickle - top_decile_alpha 0.071741, long-short naive t 2.836064, HAC t 2.619912, alpha HAC t 4.376230, monotonicity -0.890909, equal-weight +0.181371 - so the record's known insider nondeterminism did not bite. C2, right-censoring is real, counted and monotone, with dates observable h63 69, h126 68, h189 67, h252 66, h315 65, h378 64, h441 63, h504 62, i.e. EXACTLY ONE 63-day rebalance date lost per extra quarter of window and censoring removing a SUFFIX of dates rather than scattering NaNs. C3, default payloads unchanged, proved end to end on a real build. THE DEFECT NAMED IN ADVANCE AND AVOIDED: right-censoring is NOT delisting. The shipped delisting branch returns the last traded price when a name stops trading mid-window, which is correct; if it also fired when the CALENDAR ends before the window does it would return a SHORTER realized return labelled as a long-horizon one, for the most recent dates specifically, flattering short horizons and penalising long ones - which is the exact comparison this study is. Pinned from both sides by test. RESULT: R(8) = 6.1950 against the pre-registered 6.0 bar, so the shape is CONSTANT-RATE. Cumulative alpha h63 1.6464%, h126 3.3350%, h189 4.5202%, h252 6.1403%, h315 7.7910%, h378 8.6024%, h441 8.8639%, h504 10.1994%. THE SINGLE SENTENCE IS THAT ANNUALIZED TOP-DECILE ALPHA IS ESSENTIALLY FLAT FROM THREE MONTHS TO TWO YEARS: h63 6.59%, h126 6.67%, h189 6.03%, h252 6.14%, h315 6.23%, h378 5.73%, h441 5.07%, h504 5.10%. All eight incremental quarters are positive (Q1 +1.65pp, Q2 +1.69pp, Q3 +1.19pp, Q4 +1.62pp, Q5 +1.65pp, Q6 +0.81pp, Q7 +0.26pp, Q8 +1.34pp), reported with the stated limitation that adjacent cumulative windows overlap almost entirely so the inference is approximate and uncalibrated and no verdict rests on it. The alpha is well measured at EVERY horizon, alpha HAC t at the overlap-corrected lag being h63 3.7695, h126 4.3836, h189 3.2912, h252 3.1608, h315 3.4240, h378 3.3887, h441 3.4180, h504 3.8301, never below 3.16 and 3.83 at two years, so this is not a signal surviving because its error bars widened. Median per-date rank IC RISES with horizon, h63 +0.0336, h126 +0.0586, h189 +0.0709, h252 +0.0725, h315 +0.0720, h378 +0.0699, h441 +0.0711, h504 +0.0655, an independent route to the same finding that never touches the decile machinery. TWO THINGS THAT CUT AGAINST THE HEADLINE, REPORTED BECAUSE THEY DO. First, the classification clears its bar NARROWLY at 6.195 against 6.0 and DOES NOT REPLICATE ACROSS HALVES - early R(8) 8.559 which is super-linear, late 5.470 which would classify INTERMEDIATE - because R(8) is a ratio whose denominator is one noisy quarter. The two halves agree in SIGN and both show alpha still accruing at two years with alpha HAC t above 2 at nearly every horizon, so the PERSISTENCE replicates and the LABEL does not; the flat annualized-alpha column is the robust reading. Second, THE LONG-SHORT SPREAD DECAYS AND ITS SIGNIFICANCE COLLAPSES, long-short HAC t being h63 2.7167, h126 2.7111, h189 2.1446, h252 1.8561, h315 1.6495, h378 1.1632, h441 0.6619, h504 0.6846, with the cumulative spread peaking at Q5 and falling thereafter, so the persistence lives entirely in the LONG leg and nobody may quote a long-short figure beyond about a year. That asymmetry is fortunate rather than damaging because the shipped product is a LONG-ONLY hot list, but it means the long-short research statistic and the product statistic diverge with horizon and the record has been quoting them side by side. CALIBRATED BARS: X7 and session 10's floors are valid at exactly ONE configuration, h63 on the full 69-date panel at HAC lag 1, because n changes with the horizon and the windows overlap and the HAC lag changes with them, so they are NOT quoted against any other arm - the error the record already paid for twice. At that one arm the incumbent clears all three, 2.6199 vs 2.2837, 4.3762 vs 2.2913, 2.8361 vs 2.1437. PER-HORIZON PLACEBO, 200 draws, seeds 2000-2199, fixed weights and no CPCV, labelled fixed_weights_null because it is a DIFFERENT and less conservative null than X7's and its percentiles may never be compared with 2.2837 or 2.2913: 8 of 8 horizons clear their own top-decile alpha floor and 4 of 8 clear their own long-short floor. Floors h63 alpha 1.5151 ls 1.7494, h126 alpha 1.3101 ls 1.7938, h189 alpha 1.5335 ls 1.6360, h252 alpha 1.4884 ls 1.6292, h315 alpha 1.5614 ls 1.7336, h378 alpha 1.5553 ls 1.5721, h441 alpha 1.5538 ls 1.8245, h504 alpha 1.6720 ls 2.0837. C4 holds, the null median alpha HAC t is near zero at every horizon. BY-PRODUCT WITH NO VERDICT ATTACHED, registered as such: the fixed-weights null's long-short HAC p95 at h63 is 1.7494 against session 10's 2.2837 on the same panel and statistic WITH CPCV selection in the loop, a gap of +0.5343 of a t, which is a direct estimate of what adoption on a noise draw adds to a floor and is the same phenomenon X7 flagged post-hoc as roughly +1.4 of long-short t. TENURE, descriptive and charged at zero: 7,286 spells over 1,895 distinct names at a median decile size of 156. Kaplan-Meier median spell is 1 rebalance, about 3 months, and the naive median over completed spells agrees at 1.0 so censoring is not doing the work. 5,146 of 7,286 spells (70.6%) last exactly one rebalance, mean 1.568, max 19. One-period retention is 0.3660, INSIDE the 20 to 50% band the register pre-committed from the shipped 261%/yr turnover at four rebalances a year, so the tenure measurement and the cost model describe the same book and the registered BUG branch is not triggered; stable across halves at 0.3592 early and 0.3742 late. Continuous survival decays fast but RE-ENTRY IS THE NORM: allowing gaps, persistence plateaus at roughly 19 to 24% out to eight rebalances rather than decaying to zero, and 1,407 of 1,895 names have more than one spell. Exits are genuine rather than data artifacts, 6,986 falling out while still in the panel against only 115 leaving the panel and 185 censored. By market-cap tertile computed WITHIN each date, KM median is 1 rebalance in all three tiers while mean spell length is small 1.788 > mid 1.372 > large 1.224, i.e. SMALL CAPS STAY LONGEST. WHAT THIS DOES NOT SAY, and it is the most likely misuse: it is NOT a finding that the book should rebalance less often. cum_alpha(H) is the buy-and-hold return of the cohort selected on ONE date, whereas a quarterly-rebalanced book re-selects every quarter and compounds fresh selections, so those are different claims and only the first is measured. It does make the second worth testing, which it was not before, since the deployed book pays 261%/yr turnover to harvest an edge still accruing at two years - that is S23's question, needs its own pre-registration, and adopting it would be a VINTAGE EVENT that resets the five-year clock for zero statistical gain. Nor does it license quoting any long-horizon number as out-of-sample evidence: it is the same panel, and the overlapping windows make the eight arms eight views of one sample rather than eight samples. THE PRE-REGISTERED EXPECTATIONS WERE WRONG ON BOTH HEADLINE QUESTIONS: SATURATING was predicted at 60/40 and the answer is CONSTANT-RATE; large-cap tenure was predicted longer at 55/45 and small is longest; delta_k was predicted not reliably positive beyond k=4 at 60/40 and all eight quarters are positive. Right: KM median tenure at most 2 rebalances, predicted 65/35, and C1 reproducing, predicted 95/5. Equity N 135 -> 143, charged as eight horizon arms including h63 because a sweep from which a best horizon COULD be quoted is a search over eight cells regardless of which one was already known, and understating N overstates the significance of every DSR-gated claim. The tenure statistics, the placebo calibration and the half-splits are charged at zero as registered. |
| ARTIFACT-N18 | 2026-08-10 | infra | n/a | Charging S22's eight horizon arms moves equity N 135 -> 143, so the canonical BACKTEST_RESULTS.json ships a Deflated Sharpe computed at the previous, flatteringly smaller denominator - the same silent staleness sessions 13, 14, 15 and 17 each had to repair, re-created by this session's own row | the artifact's deflated_sharpe must equal research_log's equity N at the current count, with every other published figure unchanged | FIXED | n=1 | HANDOFF_edge_audit.md session 18. Full re-run on the 2,531-name / 69-date universe from a CLEAN tree at 6d9aac7, git.dirty false. DSR 0.8504129179654681 at n_trials 135 -> 0.8436955925493782 at 143; sr0_benchmark 0.43286698556205233 -> 0.43607688750783635; metric still self-reports deflated_sharpe_ratio with is_effectively_undeflated false; sqrt(2 ln 143) = 3.1505, still above the Harvey-Liu-Zhu hurdle of 3.0. NOTHING ELSE MOVED: leaf-by-leaf diff over 1,217 leaves gives 16 moved / 0 ADDED / 0 REMOVED - five ARE the DSR chain (value, probability, n_trials, n_trials_from_research_log, sr0_benchmark), four are provenance (generated_at, generated_at_utc, git.commit, git.short) and the remaining seven moved by less than 1e-9 relative, i.e. last-digit float, in cost fields (two realised_one_way_bps, one breakeven_one_way_bps and four cost-curve entries). long_short_tstat 2.8360640685320595, top_decile_alpha 0.07174142332098163, monotonicity -0.8909090909090909, long_short_tstat_nw 2.6199121240414884 and equal_weight_ann 0.18137118752419476 are all BIT-IDENTICAL, universe is 2531 / 69 label full, errors is empty so no validation block degraded, and cpcv.adopt is still false so the shipped strategy takes no haircut. Re-run, never hand-patched - patching this artifact is the one repair the file's own history forbids. The run survived the ~6.0 GB point at which session 17's attempt died silently. |
| TP-BAR | 2026-08-11 | options | yes | Raising the options take-profit from +100% to +150% (tp150) or +200% (tp200) improves per-trade expectancy on the paper book by enough to justify changing the shipped exit. Item A of HANDOFF_parked_positives.md had been refused by MIN_EXPECTANCY_GAIN = +10pp, a constant set for adopting a CONSTRUCTION change, and PREREG_A_take_profit_bar.md argued from the distribution that the bar was mis-specified in SHAPE and not merely in level. Don chose Option 2 on 2026-08-11: decide against a bar that is MEASURED rather than chosen | Four conditions all pre-committed in PREREG_A_take_profit_bar.md section 3 BEFORE any of them was evaluated, and the bar itself committed alone at e8e5505 with the scoring module not yet written. C1 the bar is the p95 of a null built by jittering the arm's own family - target 0.50-2.00, stop -0.30 to -0.70, time fraction 0.25-1.00, endpoints read off options_exitlab.POLICIES, n=100, seeds 1000-1099, identical frozen paths and identical apply_arm - BINDING whatever it returns including a bar above +3.82pp; C2 the gain must stay positive after winsorising the top 1% of per-trade differences; C3 the gain must hold on the five pooled random-entry seeds; C4 the existing FDR, both-halves, both-entry-sets and PBO gates. ADOPT requires all four; failing any one is REJECTED, no third state | REJECTED | n=2 | HANDOFF_optionsbot.md session 22; PREREG_A_take_profit_bar.md sections 8-9; scripts/tp_bar.py, scripts/tp_bar_score.py, tests/test_tp_bar.py (16 tests), tests/test_tp_bar_score.py (14 tests); data/options_pathstudy/TPBAR_NULL.json (all 100 draws retained) and TPBAR_VERDICT.json. THE CALIBRATED BAR IS +5.0812pp. Null min -6.786, p5 -4.570, median +0.803, max +8.111, and 53 of 100 draws beat the shipped exit. BOTH ARMS FAIL C1 AND ONLY C1: tp150 gains +3.1948pp, the 82nd PERCENTILE of its own family against a 95th-percentile bar; tp200 +3.8238pp at the 87th. C2 PASSES against the memo's own prediction that it would be the hazard - the cap lands at +124.3pp, 39 trades are capped, and tp150 still retains 77% of its gain (+3.19 to +2.45pp), so the raised target is NOT tail-driven. C3 PASSES (+2.5969pp on the five pooled random seeds). C4 PASSES (FDR discovery, paired name-year sign z +7.93 signal and +8.09 random, both halves positive on both books, PBO 0.00). THE HARNESS REPRODUCES THE RECORD INDEPENDENTLY, which is what makes the verdict quotable: this scorer was written for the path study rather than for O1 and returns shipped at +3.410308%/trade with gains of +3.1948 and +3.8238pp against O1's banked expectancy_diff of 0.031947661 and 0.038237652 - the same figures to four decimals from a separately-written path rebuild. WHAT THE REJECTION DOES NOT SAY: it does not say raising the take-profit does nothing, since every non-calibrated condition says the effect is real, broad and entry-independent. It says the arm is not DISTINGUISHED WITHIN ITS OWN FAMILY - 53 of 100 arbitrary jitters beat the shipped rule and the best five beat it by +5.2 to +8.1pp, all the same wider-stop-higher-target-longer-hold shape - and when a whole REGION of parameter space looks good on one corpus, that corpus cannot say where inside it the optimum lies, so picking one point from it after five looks is choosing by hindsight. THE OBJECTION IS STATED RATHER THAN LEFT FOR A READER TO FIND: a narrower null would give a lower bar and tp150 might clear it, which is precisely why the construction was fixed before it ran and bound whatever it returned, since choosing the null after seeing which one passes the arm is the move the whole document exists to refuse. BY-PRODUCT UNFLATTERING TO THE INHERITED RULE, reported because it cuts that way: the shipped +100%/-50%/half-DTE exit sits slightly BELOW the median of its own family, beaten by 53 of 100 jitters at +0.803pp, so it is an ordinary member rather than a local optimum - and it cannot be acted on for exactly the reason tp150 was refused, so this exercise must never be quoted as having validated it. THE PRE-REGISTERED EXPECTATION WAS WRONG THREE TIMES OUT OF THREE: the bar was called at +1 to +4pp (60/40) and landed +5.0812; tp150 was called to clear (55/45) and failed; tp200 was called to clear while failing C2 (50/50) and instead failed C1 while PASSING C2, as did tp150. NO POLICY CHANGED - the paper options book keeps +100% target, -50% stop, half-DTE time stop, and item A is CLOSED rather than re-parked. The dead-entry caveat travels: R2 stands, the alert book loses to random entry by -6.65pp, and no exit rule makes it tradeable. Charged 2 for the two winsorised re-scores as committed in the memo; the C1 calibration is charged ZERO on the X7 and session-10 HAC-floor precedent, since a calibration searches nothing. Options N 205 -> 207. Equity N is untouched BY THIS WORK but is no longer 135 - it reads 143, moved by S22's eight horizon arms landing from another lane while this ran, and is recorded here because a stale N overstates the significance of every DSR-gated claim. |
| S23 | 2026-08-11 | equity | yes | The live equity book holds a name until it falls out of the top 50 with a two-period minimum hold, and that exit has never been tested against an alternative - an inherited rule like the 63-day horizon S22 found was never a measured optimum. Does a price-based exit - reaching point-in-time fair value, or a conventional take-profit / stop-loss - beat it, and what does never selling cost | PREREG_s23_exit_rule.md, committed ALONE at 6a73485 BEFORE scripts/exit_rule.py existed and a strict git ancestor of the measurement commit 3ba5f4d. One buy rule (top 25 by composite) and min_hold 2 IDENTICAL across every arm so churn protection is not a confound; only the exit differs. Five exits plus a control: A0 incumbent rank-exit at 2N; A1 fair value point, sell when gap = ln(fv/price) <= 0; A2 fair value lens band, sell when price reaches the LOWEST lens the engine actually produced (dcf_ps, comps_fv or growth_ps), i.e. the conservative edge of the engine's OWN disagreement rather than a band width chosen here; A3 O'Neil/CANSLIM +25/-8; A4 textbook 2-to-1 +20/-10; C-NEVER, a CONTROL and not a candidate, which never sells and whose book therefore grows without bound. BOTH TP/SL PAIRS ARE NAMED FROM PUBLISHED CONVENTION IN THE REGISTER, BEFORE ANY RUN, AND NO GRID IS SWEPT - sweep_hold_params already sweeps trailing_stop over four values and picking the best cell is the in-search +8.43%/yr to locked hold-out -0.04%/yr failure this project already paid for. Fair values are rebuilt through the LIVE engine (calibration.pit_company + lean_fair_value, the path pinned by test_lean_path_matches_the_full_pipeline), never re-implemented. Costs charged at 33.4 bps one-way, the MEASURED realised cost from B11, as bps/1e4 x (bought + sold) / held, which is exact for an equal-weighted book, and the NET figure carries the verdict because an exit rule's whole cost is turnover. Primary statistic is the PAIRED per-period net difference against the incumbent with a HAC lag-1 t, because every arm is scored on the same panel and the same dates so the market move cancels. A challenger BEATS only if the paired difference is positive AND clears its OWN placebo floor AND agrees in sign across both halves | NO CHALLENGER BEATS THE INCUMBENT - AND NEVER SELLING IS THE ONLY MEASURABLE EFFECT | n=6 | HANDOFF_edge_audit.md session 19; data/free_analysis/EXIT_RULE.json; scripts/exit_rule.py; tests/test_edge.py (9 new S23 tests). Corrected 2,531-name / 69-date factor panel and a 108,241-row point-in-time valuation panel on the SAME 69 dates. RESULT: A1_FV_POINT -0.13pp/yr at HAC t -0.3681, A2_FV_LENSBAND +0.37pp/yr at HAC t +0.8655, A3_TPSL_ONEIL -0.21pp/yr at HAC t -0.3958, A4_TPSL_2TO1 -0.13pp/yr at HAC t -0.2544, C_NEVER -10.89pp/yr at HAC t -3.8013. All four price-based exits move the book by less than 0.4pp/yr in either direction on 69 paired periods with abs(HAC t) at most 0.87, so none is separable from zero. THREE OF THE FOUR FLIP SIGN BETWEEN HALVES: A1_FV_POINT early -0.22pp late -0.02pp; A2_FV_LENSBAND early -0.31pp late +1.04pp; A3_TPSL_ONEIL early -0.72pp late +0.21pp; A4_TPSL_2TO1 early -0.65pp late +0.30pp; C_NEVER early -2.09pp late -18.07pp - and the ONLY arm with a positive full-sample difference, A2 at +0.37pp/yr, is exactly the one that fails the both-halves requirement (negative early, positive late), which is session 7's LOO pattern again and precisely what that requirement exists to catch. THE CONTROL IS THE ONE LARGE AND WELL-MEASURED RESULT AND IT CONFIRMS S22 RATHER THAN CONTRADICTING IT: never selling costs -10.89pp/yr at HAC t -3.8013, the book grows to 417 names and alpha versus the equal-weighted universe collapses from 15.48% to 3.37%. That is NOT evidence against S22's persistence finding: S22 measured the forward return of a cohort selected on ONE date and found it beats the universe for about eight quarters, whereas a never-sell book keeps BUYING and so accumulates cohorts of every age - over 69 rebalances the average holding is far older than eight quarters and the book converges toward a slice of the universe whose alpha must approach the universe's own. Costs are not the story either, since C-NEVER's gross 20.61% barely differs from its net because it never sells; the mechanism is DILUTION, not friction, which is why the register labelled it a control and not a candidate in advance. CALIBRATED FLOOR BUILT RATHER THAN BORROWED, because X7 and session 10 calibrate quantile_backtest on the full-universe DECILE book and _backtest_hold is a different object: 200 placebo draws, seeds 3000-3199, through the identical arms, giving per-arm p95 floors of A1_FV_POINT 1.9181, A2_FV_LENSBAND 1.8518, A3_TPSL_ONEIL 2.0260, A4_TPSL_2TO1 2.0489, C_NEVER 2.5543; labelled fixed_weights_null and NOT comparable with 2.2837 or 2.2913. CONTROLS ALL PASS. C1, the valuation panel lands on the factor panel's dates EXACTLY (69 dates, 2009-01-15 to 2026-01-28) and covers 93.9% of its rows. C2, ZERO network calls during the valuation build, enforced by a tripwire that raises. C3, the incumbent reproduces the shipped _backtest_hold to sixteen digits, total_return 158.09816857704106. C4, every arm scores identical dates and target_n. C6, net is at most gross for every arm and the turnover ranking and the drag ranking are IDENTICAL. Every exit rule genuinely fires, so no arm is a silent copy of the incumbent. Fair-value coverage reported before any verdict per the COVERAGE RULE: 108,100 of 108,241 rows valuable, the point gate firing on 60,526 and the lens gate on 76,320. THE LEVELS ARE NOT A NEW HEADLINE: _backtest_hold is the concentrated top-25-to-50 book audit B17 already labels the noisiest number in the results file, whose realised size is about exit_rank rather than top_n. This register measures DIFFERENCES between exit rules on that object; the incumbent's own level is not a claim it makes. THE TP/SL LIMITATION TRAVELS WITH THE ARMS: the panel carries no intra-quarter path, so a name that touches +40% mid-quarter and gives it back is never seen to hit a +25% take-profit, and those rules therefore trigger LESS often than real ones - which biases the measurement IN FAVOUR of the TP/SL arms and makes a negative result on them the trustworthy direction. EXPECTATIONS: no challenger beating the incumbent was predicted at 75/25 and is RIGHT; TP/SL being worse than the fair-value arms was predicted at 60/40 and is RIGHT directionally though every margin is inside noise; all four price exits being negative was predicted at 70/30 and is WRONG since A2 is positive; C-NEVER beating the incumbent gross was predicted at 55/45 and is WRONG by 13.5pp; fair-value coverage below 90% was predicted at 60/40 and is WRONG at 93.9%; and whether the stop-loss or the take-profit does the damage is recorded UNRESOLVED rather than scored, because A3 and A4 differ in BOTH legs at once and adding a TP-only or SL-only arm after seeing this result is exactly the grid search the register forbids. Equity N 143 -> 149, charged as five arms plus the control; the placebo calibration, the half-splits and the coverage diagnostics are charged at zero. ADOPTS NOTHING - adoption is a VINTAGE EVENT that resets the five-year clock for zero statistical gain and is Don's call on this evidence, not a consequence of a number clearing a bar. |
| ARTIFACT-N19 | 2026-08-11 | infra | n/a | Charging S23's six arms moves equity N 143 -> 149, so the canonical BACKTEST_RESULTS.json ships a Deflated Sharpe computed at the previous, flatteringly smaller denominator - the staleness sessions 13, 14, 15, 17 and 18 each had to repair, re-created by this session's own row | the artifact's deflated_sharpe must equal research_log's equity N at the current count, with every other published figure unchanged | FIXED | n=1 | HANDOFF_edge_audit.md session 19. Full re-run on the 2,531-name / 69-date universe from a CLEAN tree at f2ea7b4, git.dirty false. DSR 0.8436955925493782 at n_trials 143 -> 0.8388059159836208 at 149; sr0_benchmark 0.43607688750783635 -> 0.4383566440162917; metric still self-reports deflated_sharpe_ratio with is_effectively_undeflated false; sqrt(2 ln 149) = 3.1635, still above the Harvey-Liu-Zhu hurdle of 3.0. NOTHING ELSE MOVED: leaf-by-leaf over 1,217 leaves gives 20 moved / 0 ADDED / 0 REMOVED - five ARE the DSR chain, four are provenance (generated_at, generated_at_utc, git.commit, git.short) and the remaining eleven moved by less than 1e-9 relative, i.e. last-digit float, in cost and no-trade-band fields. long_short_tstat 2.8360640685320595, top_decile_alpha 0.07174142332098163, monotonicity -0.8909090909090909, long_short_tstat_nw 2.6199121240414884 and equal_weight_ann 0.18137118752419476 are all BIT-IDENTICAL, universe is 2531 / 69 label full, errors is empty and cpcv.adopt is still false so the shipped strategy takes no haircut. RE-RUN, NEVER HAND-PATCHED. THE FIRST ATTEMPT WAS KILLED MID-BUILD by the harness and wrote NOTHING, which is the correct failure mode - the artifact stayed at the previous N with clean provenance rather than being left half-written - and it was simply re-run, the same discipline session 17 applied when its attempt died at 60 of 69 dates. |
| U1 | 2026-08-11 | options | yes | Options entered on the EQUITY composite's top names beat both the dead alert entry and a random selection of the same size on the same dates - the first test of whether the edge this project actually has reaches the options book at all. VALQUO_LEDGER.md carried U1 as DO NOT RUN AS WRITTEN, reopenable only with a composite built WITHIN the options universe or with size neutralised, and this design does BOTH: every ranking is a percentile among the ~182 optionable names as of the same rebalance date, and the primary null is drawn MATCHED ON MARKET-CAP TIER per date, so U7's mechanism is subtracted rather than acknowledged | Pre-registered in PREREG_u1_composite_entry.md, committed ALONE at 7d7c414 before any U1 code existed; the two bars committed at e34dc9d with no scorer in the tree. ONE grid mined once - 182 names x 39 rebalance dates in the options window, 6,811 cells to 5,186 trades - with every arm and every null draw a SUBSET of it, so arms cannot differ by fill, contract, exit or calendar. Entry is the first trading day STRICTLY AFTER the rebalance via the U7 backward-looking join; the shipped +100/-50/half-DTE exit is CALLED not copied. Bars are the p95 of 200 shape-matched draws, TOP10 plain +7.2870pp and cap-matched +9.4513pp, BINDING whatever they returned. PASS requires ALL FOUR of V1 plain bar, V2 cap-matched bar, V3 paired date-block CI95 excluding zero, V4 positive in both halves; a negative gain or a backwards decile table is REJECTED; everything else including a near miss is NULL | REJECTED | n=3 | HANDOFF_optionsbot.md session 23; PREREG_u1_composite_entry.md sections 10-11; valuation/edge/composite_entry.py, scripts/u1_entry.py, scripts/u1_bar.py, scripts/u1_score.py; tests/test_composite_entry.py (32), tests/test_u1_bar.py (12), tests/test_u1_score.py (15); data/options_u1/U1_GRID.pkl, U1_NULL.json (all 200 draws of each null retained, both split-clean and raw), U1_VERDICT.json. RESULT: TOP10 n=486 gains MINUS 1.1892pp against the grid's +3.6516%/trade, sits at the 31st percentile of its plain null and the 15th of its cap-matched one, and FAILS ALL FOUR conditions - V3's date-block CI95 is [-11.74, +10.29]pp and V4's halves flip from +5.18 early to -5.61 late. A negative gain is REJECTED by the registered rule. THE MECHANISM IS CLEANER THAN THE VERDICT: every decile's MEDIAN trade lies between -52.5% and -54.3% - all ten of them - so the composite does not move the typical option trade at all, and every decile difference lives in a right tail that a +7.29pp bar says is noise. TOP10's mean is entirely tail-carried, its best five trades contributing +3.912pp of a +2.462% mean (158.9% of it); winsorised at p99 the gain worsens to -1.4911pp. ON R2's STANDING STATISTIC THE TOP NAMES ARE SIGNIFICANTLY WORSE: the paired name-year sign test within (ticker, year) gives TOP10 119 of 285 cells won (41.8%, z -2.7840, p 0.0054) and TOP20 210 of 489 (42.9%, z -3.1203, p 0.0018) - two arms, same direction, the wider one stronger - stated with its limit that NO calibrated bar exists for a paired within-grid sign test, so those p-values are conventional and uncalibrated. BOT10 CLEARS BOTH BARS AT THE 99TH PERCENTILE AND IS NOT A FINDING: its date-block CI includes zero, it is carried by the late half (+1.13 early vs +12.47 late), and its sign test is 52.0% at p 0.4681, so it does not win more OFTEN, it wins BIGGER. The decile table is UNORDERED, not INVERTED - D9 is the worst cell at -4.89% and D10 the best at +11.35%, which no monotone story explains - and 'the composite runs backwards' would be the exact error TP-BAR closed, reading a region of a noisy corpus as a location. E5 CONFIRMED: the mechanical quarterly grid beats the dead alert book (+3.6516% vs +3.2702%/trade) while TOP10 LOSES to it by -0.8078pp, and both sit far below the five-seed random-DAY control's +8.3342% (not a paired comparison - the control samples days across the whole year while the grid is locked to 39 fixed dates). EXPECTATIONS SCORED 3 RIGHT, 2 WRONG, 1 UNTRIGGERED: NULL predicted and REJECTED delivered, gain predicted 0 to +4pp and delivered -1.19pp, the cap-matched-binding prediction never fired because V1 failed first though its spirit held. WHAT IT DOES NOT SAY: it does not refute the equity composite, whose rank IC RISES with horizon per S22 while a 30-75 DTE contract lives at the SHORT end where it is weakest - pre-registered in section 1 as testing the composite where it is least strong - and it does not say name selection is hopeless in options, only that THIS composite at THIS horizon on THIS megacap universe carries no information about the median option trade. SEPARATELY, U1-SPLIT: option chains are as-traded and unadjusted for splits while bars are adjusted, and GE's 1-for-8 reverse split on 2021-08-02 books a +31,921% trade worth 6.28pp of the raw grid's 9.93% mean. Excluded by an EXTERNAL table and a date comparison, never by the size of a return. It moves a PUBLISHED headline: R2's gap goes -6.6468pp to -5.0640pp, so 24% of it is a corporate-action artifact, and because the control is hit ~12x harder the defect has been making R2 look WORSE than it is. R2's sign and verdict are unchanged. The repair belongs upstream in the miner and is NOT DONE. Charged 3 for the three scored arms; the mine and both calibrations charged ZERO on the X7 and session-10 precedent. Options N 207 -> 210. Equity N untouched at 149, re-measured rather than copied - it was 143 when this register opened and S23 moved it |
| SECTOR-NEUTRAL-B6 | 2026-08-11 | equity | yes | Sector-neutral ranking scores every granular number against its SECTOR median before the global z-score. It was rejected twice, by P10 on 2026-07-31 and again on 2026-08-02, in both held-out split directions and under both weightings, on the reasoning that it buys long-short t and sells top-decile alpha while Valquo trades a long-only book. BOTH REJECTIONS RAN ON THE PRE-B6 110-DATE 2710-NAME PANEL THE PROJECT HAS SINCE DECLARED VOID: the decision turned on a -1.58pp alpha difference measured inside a panel whose alpha LEVEL moved -4.18pp when the inverted-universe defect was removed. Does it clear the SAME pre-committed gate on the corrected 69-date panel | PREREG_sector_neutral_b6.md, committed ALONE at 1bdb7e0 BEFORE scripts/sector_neutral_rerun.py existed and a strict git ancestor of the measurement commit. THE GATE IS THE SHIPPED holdout_compare_panels WITH THE MARGINS ALREADY IN THE REPOSITORY: MIN_HOLDOUT_TSTAT_GAIN +0.25 long-short t and MIN_HOLDOUT_ALPHA_GAIN +100bps top-decile alpha, required in BOTH time halves with the boundary date embargoed. Two pre-specified weightings and no others: DEPLOYED, the seven themes that trade at 0.125 each, which carries the verdict because it is what ships; and FLAT, those seven plus growth and low_risk, as a robustness check on whether the answer depends on the weighting. ADOPTED iff the deployed gate returns adopt and the flat gate does not return reject; REJECTED iff the deployed gate returns reject; anything else NOT REPLICATED, and ambiguous is a null rather than a pass. ONE PANEL BUILD, TWO ARMS, A PROVABLY IDENTICAL ROW SET: each cross-section calls build_frame twice on the SAME metrics list, once flat and once sector-neutral, emitting sn_theme columns on the same row. Both prior runs built the arms as two SEPARATE runs, and this project has recorded that a full backtest is not reproducible run to run with insider moving median IC -0.0034 to +0.0155 to -0.0034 across three identical-data runs, so a per-arm build lets that land inside the difference being measured. One pass makes it common-mode. Calibrated bars quoted where they were calibrated, at the full-universe decile book on 69 dates at H=63 with HAC lag 1, and labelled an extrapolation for the sector-neutral arm. Secondary paired within-panel difference against an explicitly UNCALIBRATED 2.0, which cannot overturn the primary gate | REJECTED - AND THE TRADE-OFF THE ORIGINAL REJECTION RESTED ON DOES NOT EXIST ON THIS PANEL | n=2 | HANDOFF_edge_audit.md session 20; data/free_analysis/SECTOR_NEUTRAL_B6.json; scripts/sector_neutral_rerun.py; tests/test_edge.py (five new tests) and tests/test_sector_neutral.py (two new tests). Corrected 2,531-name 69-date panel, 113,945 rows, 2009-01-15 to 2026-01-28. RESULT UNDER THE DEPLOYED WEIGHTS: top-decile alpha +7.1741% to +6.0852%, a difference of -1.0890pp; long-short annualised +11.0382% to +8.5107%; long-short t +2.8361 to +2.3423; long-short HAC t +2.6199 to +2.1505; top-decile alpha HAC t +4.3762 to +4.0893; monotonicity -0.8909 to -0.8667. The gate fails in BOTH halves and returns reject: early half long-short t difference -0.0093 and alpha difference -0.1459pp, late half -0.4846 and -2.0144pp. Under the FLAT weights the same answer, alpha +4.9912% to +4.2194% and long-short t +1.1680 to +0.8679, gate reject, so the verdict does not rest on a choice of weighting. THE HEADLINE IS NOT THAT IT FAILED AGAIN BUT THAT IT FAILED DIFFERENTLY: on the void panel sector-neutral BOUGHT long-short t, +0.500 from 3.396 to 3.896, and sold alpha, and the rejection was a judgement that a long-only book should not make that trade. On the corrected panel the long-short gain is GONE and its sign is REVERSED, -0.4938 under the deployed weights and -0.3000 under the flat ones, so sector-neutral is now worse on BOTH metrics and no trade-off has to be adjudicated at all. THE CALIBRATED FLOOR SEPARATES THE TWO ARMS: the shipped flat arm clears the long-short HAC floor of 2.2837 at 2.6199 and the sector-neutral arm does NOT at 2.1505, while both clear the top-decile alpha HAC floor of 2.2913. PAIRED WITHIN-PANEL DIFFERENCE, the V2G construction on identical dates, deployed weights: alpha -1.0890pp/yr with a HAC t of -1.6666 against an UNCALIBRATED 2.0, long-short -2.5275pp/yr at HAC t -2.5151, both halves negative on alpha at -0.1459pp and -2.0144pp, and the late half carries it. CONTROLS ALL PASS AND NONE BLOCKED THE VERDICT. C1, the two arms have IDENTICAL (date, ticker) key sets, 113,945 rows each, 69 dates, 2,531 names. C2, the toggle is NOT inert, mean absolute composite change 0.0567 at a cross-arm correlation of 0.9836 and nine of ten themes move. C3, the flat arm reproduces the published record to the digit, alpha 0.071741423321, long-short t 2.8360640685320595, HAC 2.6199, alpha HAC 4.3762, monotonicity -0.8909090909090909, equal-weight +18.137118752419476%. C4, the COVERAGE RULE checked on the corrected panel rather than inherited from the void one: sector coverage is 100.0% of rows and names across 11 sectors, the smallest sector on any date holds 50 names and there are ZERO singleton sectors, so no name is mapped to its own median. C5, insider is untouched at exactly 0.000 because it is a rescaled percentile rather than a z-scored input. C6, median-subtraction created no new missing values, so the arms score the same names. C7, sentiment is empty at 0 non-null, so excluding it from the flat set is right. The equal-weight benchmark is identical between arms to the digit, which is the universe control. EXPECTATIONS, written down first and scored: REJECTED was predicted at 80/20 and is RIGHT; the long-short gain shrinking below +0.25 was predicted at 70/30 and is RIGHT and then some since it reversed sign; a negative alpha difference between 0 and -3pp was predicted at 75/25 and is RIGHT at -1.09pp; both halves agreeing in sign on alpha was predicted at 60/40 and is RIGHT; the paired difference not reaching an absolute t of 2.0 was predicted at 60/40 and is SPLIT, right on alpha at -1.6666 and WRONG on long-short at -2.5151. Four right and one split is an unusually good run for this project's directional calls and the stated reasoning held, that the corrected window is roughly the void panel's late portion where sector-neutral already lost on both metrics. A SEPARATE DEFECT WAS FOUND AND IS REPORTED RATHER THAN REPAIRED: cross_sectional.zscore guards a degenerate cross-section with sd == 0, but whether a constant column actually has an exactly zero pandas variance is VALUE-DEPENDENT, exact for 0.0, 50.0, 2.5, 0.125 and 0.07 and about 1e-16 for 0.9, 0.1, one third and 12.34, so for those the guard misses and zscore returns a fabricated pattern with a maximum absolute z of 1.0 built entirely from floating-point residue instead of NaN. It also corrects a claim in the record, since V2G's statement that a constant insider makes zscore return all-NaN holds only because the live insider is constant at exactly 0.0 from (50 - 50) / 25 and is not a general property. EXPOSURE MEASURED RATHER THAN ASSUMED: no theme column is constant or near-constant on any of the 69 dates and the smallest within-sector dispersion across 231 sampled cells is 0.2209, so no figure in this study or in the shipped artifact is affected. NOT REPAIRED because zscore is on the live scoring path and changing it is a scoring change and therefore a vintage event, which is not this register's to make; it is pinned by a test that fails if it is ever silently corrected. THE ITEM IS CLOSED EITHER WAY BY A CLAUSE FIXED IN ADVANCE: full sector-neutral ranking may not be re-run as a re-run, and re-opening requires either new data in the form of a genuine point-in-time sector map, ledger S25, or a materially different construction, sector-relative on the value theme alone, ledger S15, which has never been tested at all. Nothing here touches the sector column's ACCEPTED use for the max_sector_w concentration cap, which is a risk control rather than a re-ranking. Equity N 149 -> 151. ADOPTS NOTHING and CONFIG.sector_neutral is untouched at false |
| ARTIFACT-N20 | 2026-08-11 | infra | n/a | Charging SECTOR-NEUTRAL-B6's two arms moves equity N 149 -> 151, so the canonical BACKTEST_RESULTS.json ships a Deflated Sharpe computed at the previous, flatteringly smaller denominator - the staleness sessions 13, 14, 15, 17, 18 and 19 each had to repair, re-created by this session's own row unless the artifact is re-run | the tracked artifact's deflated_sharpe_detail.n_trials must equal research_log equity N, and every headline must be bit-identical since no scoring code changed | re-run from a CLEAN tree, never hand-patched | n/a | HANDOFF_edge_audit.md session 20 |
| S20 | 2026-08-11 | equity | yes | The composite standardises every granular number and every theme with a winsorized z-score and then takes a weighted SUM. Replacing that standardiser with a cross-sectional RANK - (pct_rank - 0.5) * 2, the repo's own existing convention in standardize_factors(method='rank'), which build_frame has never called - at BOTH standardisation layers should improve the book, because P6.3 measured that INFLATING the tails (median/MAD) halved the long-short t and a rank transform compresses them maximally | PREREG_s20_s21_construction.md, committed ALONE at 27af414 BEFORE scripts/construction_rerun.py existed. The SHIPPED gate holdout_compare_panels at the ALREADY-COMMITTED margins (+100bps top-decile alpha AND +0.25 long-short t, in BOTH halves, boundary embargoed), on the corrected 69-date 2531-name panel, under TWO pre-specified weightings and no others - DEPLOYED carries the verdict, FLAT tests whether the answer depends on the weighting. ADOPTED iff the deployed gate returns adopt AND flat does not reject; REJECTED iff deployed returns reject; NOT REPLICATED otherwise. Per-signal and per-theme ICs are DIAGNOSTICS and may not move the verdict in either direction, per the standing rule that a construction change is never judged by per-signal IC | REJECTED - and it fails while making the DECILES BETTER ORDERED, which is the finding. Deployed: top-decile alpha +7.1741 pct to +3.6817 pct (-3.4925pp), long-short ann +11.04 pct to +9.04 pct, long-short t 2.8361 to 2.3054, HAC 2.6199 to 2.0588, alpha HAC 4.3762 to 2.0028 - so the rank arm FAILS BOTH calibrated floors (2.2837 and 2.2913, EXTRAPOLATED for a challenger arm) where the shipped arm clears both - while monotonicity IMPROVES -0.8909 to -0.9515. Both halves worse on both metrics (early dt -0.4689 dalpha -1.32pp, late dt -0.4171 dalpha -5.62pp); flat weights reject too, so the verdict does not rest on a weighting. Paired within-panel difference -3.4925pp per yr at HAC t -2.3783 against an UNCALIBRATED 2.0 bar. The composite is NOT inert - within-date rank correlation 0.8859 (min 0.8304) and 65.2 pct of names change decile - and the top-25 book overlaps the shipped one by 14 of 25 | n=2 | HANDOFF_edge_audit.md session 21; data/free_analysis/S20_S21_CONSTRUCTION.json; scripts/construction_rerun.py; tests/test_edge.py (7 S20/S21 tests). Mechanism, stated as a hypothesis: rank discards the MAGNITUDE information that identifies the genuinely extreme names, so the ordering ACROSS deciles gets smoother while the TOP decile - which is the product - loses its edge. D1 falls 25.31 pct to 21.82 pct while D10 falls 14.27 pct to 12.78 pct. Adopts nothing; adoption would be a VINTAGE EVENT |
| S21 | 2026-08-11 | equity | yes | Winsorising before standardising should improve the composite. THE PREMISE IS WRONG AND THE REGISTER SAYS SO BEFORE THE RUN: cross_sectional.zscore ALREADY winsorises at 2 pct before computing mean and sd, at BOTH standardisation layers, so the audit item proposes the shipped behaviour. The testable form is therefore INVERTED - the challenger arm is winsorisation OFF (p=0.0, an exact no-op clip to min and max) - and an adopt verdict would mean REMOVING the shipped clipping | Identical to S20's - the same register, the same shipped gate, the same already-committed margins, the same two weightings, the same corrected panel, scored in the SAME single panel build so the known insider run-to-run nondeterminism is common-mode and cancels out of every difference. One alternative and no grid over p: sweeping p and reporting the best cell is the in-search +8.43 pct per yr to locked hold-out -0.04 pct per yr failure this project has already paid for | NOT REPLICATED - a LARGE full-sample gain that does not replicate across halves and reverses under the other weighting, which is session 7's LOO pattern for the fourth time. Deployed FULL SAMPLE every headline improves and several hugely: top-decile alpha +7.1741 pct to +9.6038 pct (+2.4296pp), long-short ann +11.04 pct to +16.63 pct, long-short t 2.8361 to 4.9395, HAC 2.6199 to 4.3612, alpha HAC 4.3762 to 4.7145, monotonicity -0.8909 to -0.9758. But the GATE splits: the late half passes (dt +2.0125, dalpha +3.69pp) and the EARLY half FAILS ON THE ALPHA MARGIN BY 17 BASIS POINTS (+0.83pp against the +1.00pp bar) while clearing the t margin at +0.7863. Under FLAT weights it REJECTS outright, both halves negative on alpha (-0.40pp, -0.47pp). The paired within-panel difference is +2.4296pp per yr at HAC t +1.9170 and +5.59pp long-short at +1.9365 - BOTH BELOW the UNCALIBRATED 2.0 bar. Ambiguous against its own threshold is a NULL, per RUN_RULES A6 | n=2 | HANDOFF_edge_audit.md session 21; same artifact and script as S20. THE CAUTION THAT MUST TRAVEL WITH THE +2.4pp: the unclipped arm's most extreme composite averages 7.14x its OWN 99th percentile against 1.64x for the shipped arm, so an unclipped z-score is a fragile estimator whose book is anchored by outliers - only 8 of the shipped top 25 names survive it. Winsorisation is also a DATA-QUALITY defence and not only a statistical choice: P7 shipped a currency bug that computed book_to_price 892 against a true 0.589, and with no clip such a row dominates the entire cross-section's mean and sd. Adopts nothing; adoption would be a VINTAGE EVENT and it QUEUES BEHIND the theme restoration rather than spending a second clock reset |
| ARTIFACT-N21 | 2026-08-11 | infra | n/a | Charging S20 and S21 moves equity N 151 -> 155, so the canonical BACKTEST_RESULTS.json ships a Deflated Sharpe computed at the previous, flatteringly smaller denominator until it is re-run | Re-run the full backtest from a VERIFIED-CLEAN tree and require git.dirty false, errors empty, every headline bit-identical and only the DSR chain and provenance leaves moved | DONE - re-run at N=155, sqrt(2 ln 155) = 3.1760 | n/a | HANDOFF_edge_audit.md session 21. Charged at n/a because refreshing an artifact searches nothing; the two trials are charged to S20 and S21 themselves |
| O13 | 2026-08-11 | options | yes | R2 measured that the options alert's day-selection subtracts value, a split-clean gap of -5.0640pp per trade against a five-seed random-entry control, and the path study found the control recovering more often at every touch level on both measures while the drawdowns were identical. WHERE does that anti-signal live across the alert's own features, is it CONCENTRATED in some of them or DIFFUSE across all of them, and does the INVERSE of it carry information or is it only noise plus costs. R2's verdict is NOT re-opened; this characterises the corpse | PREREG_o13_antisignal.md, committed ALONE with PREREG_o12_kelly_ruin.md at b0f287d with no .py in the commit and a strict git ancestor of every measurement commit. Split-clean banked books only, no re-mine, exit policy untouched. TWO TRACKS forced by a schema fact measured and disclosed before the register was written: the control carries the alert's own features at 0.0 pct coverage, so the 8 alert features reach it only through the _control_for back-link at 99.7 pct with zero ambiguous rows, while the 9 structural features are on both books directly. Numeric features cut into quintiles at ALERT-book breakpoints applied unchanged to the control; categoricals at levels with at least 100 alert trades. CONCENTRATION STATISTIC fixed in advance, S_worst, the share of the rate component carried by its single worst bin, 0.20 when perfectly diffuse over five bins and 1.00 when perfectly concentrated. CALIBRATED BAR is each feature's OWN p95 over 2000 draws of a null that holds every row's book and return fixed and permutes bin labels WITHIN book, which preserves the total gap EXACTLY and destroys only the feature-to-return association. CONCENTRATED iff at least one feature clears its own p95 in BOTH halves independently, DIFFUSE otherwise. INVERSE CARRIES INFORMATION iff a decide-half refusal rule improves the measure-half gap by at least +1.50pp in BOTH directions and the refused bins really lose in both; a one-direction pass is a NULL | DIFFUSE, and the INVERSE IS A NULL | n=33 | HANDOFF_optionsbot.md section 24; data/free_analysis/O13_ANTISIGNAL.json; scripts/o13_decompose.py; valuation/edge/antisignal.py; tests/test_antisignal.py 35 tests. THE PRIMARY FINDING IS NOT THE VERDICT BUT THE DECOMPOSITION: the gap is ENTIRELY a within-bin RATE effect and essentially none of it is composition. Across all 32 arms the rate component sits between -4.23pp and -5.79pp against a total gap of -5.0640pp, while the largest mix component over all 32 is 0.7711pp, i.e. 15.2 pct of the gap, and most are under 0.2pp. The alert does not lose because it picks different CONTRACTS from random entry; it loses inside every kind of contract it picks. Q2 DIFFUSE: nothing clears its own calibrated p95 in both halves. Four of 32 arms clear on the full sample, labels Uptrend, labels Low IV 18 pct, labels Volume surge 1.6x and pit_atm_oi, against about 1.6 expected at a 5 pct bar over 32 correlated arms, and none survives the both-halves requirement. Q3a NULL and it fails in the informative direction: early-decides selects dte and refuses its q2 bin, giving -0.0977pp on the late half, and late-decides selects iv and refuses its q3 bin, giving -0.7774pp on the early half. BOTH refusal rules make the measure half WORSE, and the two directions select DIFFERENT features, which is session 7's LOO pattern and session 11's ML-combiner pattern for the third time. The iv q3 bin is the WORST bin on the late half and nearly the BEST on the early half at -1.57pp against a -5.43pp baseline. Q3b carries NO verdict by construction and is arithmetic: the anti-signal is RELATIVE to the control and not absolute, since the alert book's own expectancy is POSITIVE at +3.2702 pct per trade, so mechanically reversing it is negative before any cost and a round-trip spread is paid on top. EXPECTATIONS scored 2 right and 1 wrong of 3 scored: DIFFUSE was predicted at 65/35 and is RIGHT, the inverse failing out-of-sample was predicted at 70/30 and is RIGHT, and the prediction that no cut of the alert book loses money outright was predicted at 55/45 and is WRONG, since 17 of 100 bins have negative alert expectancy and the worst are the widest-spread and least liquid ones, entry_spread_pct q5 at -7.41 pct and pit_median_spread_pct q5 at -5.53 pct. THREE DEFECTS FOUND AND REPORTED. iv_rank is wired and 0.0 pct populated on BOTH books, a COVERAGE-RULE-class defect, and is excluded for having no values rather than for scoring badly. opt_right and horizon are CONSTANT, so the banked options book is 100 pct calls and 100 pct swing horizon and those two arms can never clear a bar; they are flagged degenerate rather than reported as failures, because did-not-clear would otherwise read as evidence about calls versus puts in a book containing no puts. The alert label vocabulary is PARAMETERISED, with the threshold baked into the label text as Low IV 14 pct through 19 pct and Volume surge 1.5x through 1.9x, so one concept becomes many near-duplicate arms and the charged arm count rises from the registered 17 to 32; the registered definition is kept and the TRIAL COST is corrected UPWARD, which runs against this register's own result. Options N 210 to 243 for this row. Equity N is untouched and BACKTEST_RESULTS.json needs no re-run, because the trial counter is domain-scoped |
| O12 | 2026-08-11 | options | yes | What position size does the options paper book's measured payoff distribution imply, where does ruin live, and what does the current flat one-contract sizing imply. GOVERNED BY A CAVEAT THAT IS PART OF THE ITEM: Kelly requires an edge that is real and R2 says this entry is dead, so every fraction here is conditional on a distribution already shown to be worse than random entry on the same names, and nothing here is a sizing recommendation for real money | PREREG_o12_kelly_ruin.md, committed ALONE with PREREG_o13_antisignal.md at b0f287d before any measurement code existed. f star maximises the EMPIRICAL log-growth, the mean of log of one plus f times R, rather than a two-outcome approximation, which would be badly wrong on a barbell whose median trade is -52 pct and whose best is +782 pct. Fixed grid from 0.0005 in 0.0005 steps plus golden-section refinement, fixed in advance and not tuned. Ruin by month-block bootstrap per R3's clustering rule at pre-committed thresholds of terminal below 0.5x and 0.2x and drawdown over 50 pct and 80 pct, 10000 paths per fraction. VERDICT RULE, the only verdict this item claims: USABLE iff f star is positive on BOTH calendar halves AND they agree within a factor of 2.0 AND the month-block bootstrap CI95 of the full-sample f star excludes zero; NOT USABLE otherwise. Ruin and flat-sizing figures are DESCRIPTIVE and carry no verdict | NOT USABLE - and the halves AGREED, so the verdict turns on the bootstrap and not on the instability that was predicted | n=3 | HANDOFF_optionsbot.md section 25; data/free_analysis/O12_KELLY_RUIN.json; scripts/o12_kelly.py; valuation/edge/kelly.py; tests/test_kelly.py 28 tests. Split-clean alert book, n 3870, mean +3.2702 pct, median -52.22 pct, hit rate 35.27 pct. THE BRIEF'S 37 pct HIT RATE DOES NOT REPRODUCE on either book, 35.32 pct as-published and 35.27 pct split-clean, and the measured value is used throughout. f star 0.0403 full sample, 0.0515 early and 0.0293 late for a ratio of 1.758 which CLEARS the pre-committed 2.0, but the month-block bootstrap CI95 is 0.0000 to 0.1001 with 8.0 pct of the 400 draws returning exactly zero, so the CI includes zero and the verdict is NOT USABLE. THE PRACTICAL HEADLINE IS THE RUIN CURVE, and it is brutal for a book whose median terminal wealth is only 1.267 after a year of 397 trades: at full f star the probability of a drawdown worse than 50 pct is 0.753, at half Kelly 0.222 and at quarter Kelly 0.006, while at f equals 0.10, which is only 2.5 times f star, the MEDIAN terminal wealth is 0.726, i.e. a positive-expectancy book LOSES money through over-betting, and at f equals 0.25 terminal wealth is 0.003. THE SIZING RESTATEMENT OF R2 IS THE MOST USEFUL LINE HERE: the random-entry control's distribution supports f star 0.1110, which is 2.75 times what the alert book supports, so the dead entry signal costs not only expectancy but the position size the book can carry. Hard upper bound f below 0.98576 is ARITHMETIC and not a finding, since the worst trade returns -101.44 pct, a total loss of premium plus commission which was checked and is correct accounting rather than a bug. Live flat sizing is one contract at a median premium of 2.57 dollars, a 257 dollar stake, so flat sizing equals f star at an account of 6371 dollars, half Kelly at 12743 and quarter Kelly at 25486. EXPECTATIONS scored 3 right, 1 right with a caveat and 1 wrong on its mechanism: f star under 0.10 predicted at 60/40 is RIGHT at 0.0403; the control's f star exceeding the alert's predicted at 75/25 is RIGHT and by 2.75 times; a probability of a drawdown over 50 pct above 0.5 at full Kelly predicted at 70/30 is RIGHT at 0.753; flat sizing implying a fraction BELOW f star predicted at 55/45 is RIGHT for five of six account sizes on the grid and WRONG below about 6371 dollars where flat sizing is 1.27 times Kelly; and the prediction that the halves would DISAGREE by more than 2x and thereby drive the verdict, made at 60/40, is WRONG, since they agreed at 1.758 and the verdict is carried by the bootstrap instead, so the right answer was reached for the wrong reason. A CONCEPTUAL POINT PINNED BY TEST rather than left to the reader: at any fraction below the hard bound literal ruin is UNREACHABLE, because a total loss multiplies wealth by one minus f which is strictly positive, so an all-losses path decays geometrically toward zero without arriving, and a ruin study counting bankruptcies would have reported that this book can never be ruined. Sequential compounding also UNDERSTATES a concurrent book's drawdown and that caveat ships inside the payload rather than in prose. Options N 243 to 246. Equity N untouched at 155 and BACKTEST_RESULTS.json needs no re-run |
| M2 | 2026-08-11 | infra | yes | Clustered (date-block / HAC) inference should be the DEFAULT everywhere a cross-date statistic is computed, with the naive i.i.d. figure available as a labelled diagnostic and n_eff reported beside n - WITHOUT re-quoting anything, because long_short_tstat is the key holdout_compare_panels' pre-committed +0.25 margin was calibrated against and the placebo floors are specific to both the statistic and the lag | one shared definition; delegations bit-identical; no existing key changes meaning; rho and n_eff reported for the long-short spread and every theme IC | DONE - infrastructure, searches nothing | n/a | PREREG_m2_m6.md committed ALONE at af88533; HANDOFF_edge_audit.md session 22; valuation/edge/statistics.py; tests/test_edge.py. Charged n/a because a refactor searches nothing: equity N stays 155. PERIOD-LEVEL EQUITY HALF ONLY - R3 had already delivered the whole trade-level half. Delegations verified BIT-IDENTICAL over 400 random series, max abs diff 0.000e+00. theme_ic and per_signal_ic gained a clustered figure they had never had, and the theme IC t is the statistic carrying X7's 2.71 bar; ic_tstat is untouched so the bar still applies to the number it was calibrated on, and the new ic_inference.t has NO calibrated floor and may not be compared to 2.71. MEASURED: long-short rho +0.189046 (reproducing R9's +0.189), n_eff 47.06 of 69; top-decile alpha rho +0.081237, n_eff 58.63. REPORTED BECAUSE IT CUTS AGAINST THE CHANGE: the theme IC series are NOT materially autocorrelated - clustered below naive in only 5 of 9, max abs rho 0.164, four of nine NEGATIVE - so the pre-registered expectation was WRONG and the closed gap is completeness rather than a moved number. The Schwert lag (3 at n=69) and M2's own CPCV-embargo fix are both REPORTED NOT ADOPTED because each would move published figures. |
| M6 | 2026-08-11 | infra | yes | A results-file schema assertion should fail the run when a COMPUTED field is silently dropped from the payload - the fixed-field-list class had already eaten a computed t-stat (R9's top_decile_alpha_tstat, 4.5174, recorded as None) and a refusal flag (archive_scan storing fair_value but not why it was blank), and a human caught both by reading two files side by side | the guard fires on both known-bad fixtures and passes on a complete payload | DONE - infrastructure, searches nothing | n/a | PREREG_m2_m6.md; HANDOFF_edge_audit.md session 22; valuation/edge/payload_schema.py; tests/test_edge.py. Charged n/a: a guard searches nothing. FIELD-LEVEL HALF ONLY - B22 closed the block level and the options-bot lane had already reported the remaining scope correctly. Found FIVE live drops, TEN fields, none previously known: B17's entire hold-book disclosure (so the canonical file now carries held_median = 42 for the book labelled 'top 25'), session 12's adopt_detail/challenger_weights_cols, and ev_freshness.rows. THE TENTH FIELD IS THE BEST EVIDENCE FOR THE GUARD: it was found BY THE GUARD on its first real run and had escaped the hand-built spec because that spec was derived from each producer's AST and ev_freshness builds its dict incrementally rather than returning a literal - static analysis could not see it, a runtime producer-enumerating guard could. Its own worst defect was caught before shipping: main()'s blanket except would have swallowed it, so a check that cannot fail anything is not a check; it now has its own handler ordered ahead of the blanket one and exits non-zero. |
| O21 | 2026-08-11 | options | yes | The option pricer ignores dividends and early exercise. Quantify what that costs on the banked book, with deep-ITM calls near ex-div as the known case, and fix the pricer if the error is material | PREREG_o21_dividends.md, committed ALONE with PREREG_o26_bucket_floor.md at bf5324c with no .py in the commit. Scoped by a fact measured and disclosed BEFORE the register was written: bs_price, implied_vol and greeks ALREADY take a continuous dividend yield q and handle it correctly, so the defect is not a missing model but that every caller uses the default q of 0.0. The banked P&L comes from QUOTED bid and ask in the frozen chains and never from a model price, so the pricer cannot move it directly; it reaches the book through exactly three doors, measured separately. D1 early exercise, the sim always SELLS at the bid while a deep-ITM American call near ex-div can be worth more exercised. D2 contract selection, pick_contract takes the contract whose absolute delta is nearest 0.35 and delta is computed at q of zero. D3 stored derived fields. EARLY EXERCISE IS MEASURED MODEL-FREE, deliberately: the textbook condition compares the dividend to remaining time value, which would make the answer a function of the very pricer under test, so the test is simply whether the booked exit price is below intrinsic. PRIMARY yield is trailing-twelve-month dividends over spot, strictly point-in-time; a scheduled-dividends variant is defined but forbidden from carrying a verdict because it reads the contract's future. MATERIALITY BAR fixed in advance: FIX the pricer if the combined effect on per-trade expectancy is at least 1.00pp in absolute value, OR if any published verdict changes its relationship to its bar, with the second clause governing even if the first fails | IMMATERIAL ON THE MEASURABLE PART, and the D2 contribution is UNRESOLVED rather than zero - the pricer is deliberately NOT changed | n=1 | HANDOFF_optionsbot.md section 29; data/free_analysis/O21_DIVIDENDS.json; scripts/o21_dividends.py; valuation/edge/dividends.py; tests/test_dividends.py 27 tests. EXPOSURE IS REAL AND WIDESPREAD: 81.4 pct of the 3,870 banked trades sit on dividend payers at a median trailing yield of 2.02 pct and a maximum of 24.8 pct, and 2,107 of 3,870 calls, 54.4 pct, span an ex-dividend date. THE COST IS NOT. D1: only 34 of 3,870 exits, 0.879 pct, were booked below intrinsic, worth +0.2002pp of per-trade expectancy against a 1.00pp bar. TWO CONTROLS, BOTH RUN BEFORE ANY FIGURE WAS QUOTED: put-call parity recovers the stored underlying_entry with a median relative error of 0.00232 and 93.2 pct within one percent, and the bars raw_close the simulation itself uses reproduces it with a median relative error of 0.00000 and 100.0 pct within one percent. D2: the q equals zero arm reproduces the banked contract on 3,870 of 3,870 entries, exactly 100 pct, which is what makes the challenger quotable, and the corrected pricer selects a DIFFERENT contract on 179 entries, 4.63 pct. The substitution is NOT a near-substitute, which makes the unresolved part more serious rather than less: median absolute delta gap 0.129 against a 0.35 target, p90 0.225, 87.7 pct keep the same expiry and 93.9 pct move to a LOWER strike, which is the predicted direction since a positive q lowers call delta and the selector must go further in the money to reach 0.35. D2'S P&L IS NOT COMPUTABLE ON THE FROZEN BOOK AND THAT IS REPORTED RATHER THAN ASSUMED TO BE ZERO: the freeze stores the FULL chain only on ENTRY dates and just the traded contract thereafter, so a contract the book never held has no forward price path - the median alternative has 2 chain dates and only 10.1 pct have more than three. Resolving it needs a re-mine, which this session was scoped out of. D3: ignoring dividends UNDERSTATES the solved implied vol by a median 0.00617, about 0.62 vol points, and OVERSTATES absolute delta by a median 0.00668, with maxima of 0.0998 and 0.146; the path study's delta85 arm moves from firing on 6 trades to firing on 2 of 3,870, which cannot disturb its recorded -0.02pp rejection. THE PRICER IS DELIBERATELY NOT CHANGED, because neither clause of the pre-registered bar is met and shipping a selection change on an unmet bar is exactly what the register exists to prevent - passing q into pick_contract would alter WHICH CONTRACT the live engine buys on 4.63 pct of entries, which is a construction change and not a bug fix. That non-change is PINNED by two tests that fail if the live selection path ever silently starts passing a yield, the same discipline session 16 used for the sizing quantity and session 20 for zscore. TWO DEFECTS IN MY OWN INSTRUMENT, both caught before any verdict was read and both pinned by tests. First, spot at exit was initially estimated as the maximum of call bid plus strike over the chain; parity gives call at least spot minus strike, so every bid plus strike is an UPPER bound on spot and the maximum is the loosest one available, which inflated intrinsic and reported an early-exercise gain of +5.62pp on a smoke subset - eight times the true figure and in the direction that would have manufactured a material finding. Replaced by put-call parity, an equality rather than a bound. Second, the parity helper then rejected a put mid at or below zero, which discards exactly the deep-ITM-call cases early exercise lives in and scored ZERO rows. Also found: options_backtest.BARS_CACHE is a RELATIVE path, so it silently resolves to nothing from a git worktree and returned an empty bar set rather than an error. EXPECTATIONS scored 4 right and 1 wrong of 5 scored: IMMATERIAL predicted at 70/30 is RIGHT on the measurable part; fewer than 2 pct of exits below intrinsic predicted at 65/35 is RIGHT at 0.879 pct; selection changing on fewer than 10 pct of entries predicted at 55/45 is RIGHT at 4.63 pct; the delta85 firing rate moving by less than 1pp predicted at 75/25 is RIGHT at 0.10pp; and the implied-vol shift being NEGATIVE is WRONG AS STATED - the register did not state the direction of subtraction, and the underlying arithmetic it gave is confirmed, namely that ignoring dividends understates the solved vol, so the shift measured as corrected minus published is POSITIVE. TRIAL COST CORRECTED UPWARD MID-SESSION: this row was first charged n=0 on the reasoning that a correctness measurement is a FIXED-class row, and the research-page suite rejected it - the log schema requires every non-FIXED row to charge at least one trial, and this row is not FIXED because nothing was repaired. It had a pre-committed bar and returned a verdict against it, so it is a trial. Options N 246 -> 247 for this row |
| O26 | 2026-08-11 | options | yes | options_tracker.MIN_CLOSED_PER_BUCKET is 30 and gates whether a subgroup may be tuned on or reported as judgeable. Its own comment states the purpose - enough that one lucky contract cannot flip the verdict - and that property has never been measured. At what bucket size does it actually hold | PREREG_o26_bucket_floor.md, committed ALONE with PREREG_o21_dividends.md at bf5324c before any measurement code existed. PRIMARY STATISTIC is the literal operationalisation of the constant's own comment: P_flip of n, the probability that removing the SINGLE most extreme trade flips the sign of a size-n bucket's mean. The removed trade is argmax of the absolute deviation from the mean, chosen WITHOUT reference to which way it moves the mean - removing the best trade instead would build the answer's direction into its own definition. Estimated by drawing size-n buckets with replacement from the split-clean alert book, 5000 draws per n, over a grid of 10, 20, 30, 40, 50, 75, 100, 150, 200 and 300 fixed in advance and not extendable after seeing the curve. FLOOR is the smallest n on the grid with P_flip at most 0.05. RAISE iff the floor computed independently on the early and late calendar halves agrees within one grid step and exceeds 30; KEEP 30 iff the floor is at most 30 on both halves; NULL otherwise, and a NULL keeps the shipped value so the failure direction is always keep 30 rather than adopt an unvalidated number. SECONDARY and independent: half-to-half sign agreement of bucket expectancy, reported separately rather than folded into the primary | NULL, so the floor STAYS 30 - but not because 30 is adequate; because NO floor available to this book delivers the property the constant claims | n=1 | HANDOFF_optionsbot.md section 30; data/free_analysis/O26_BUCKET_FLOOR.json; scripts/o26_floor.py; valuation/edge/bucket_floor.py; tests/test_bucket_floor.py 19 tests. P_flip NEVER REACHES THE 0.05 BAR ANYWHERE ON THE GRID, on the full sample or on either half: 0.2226 at n=10, 0.1848 at the shipped n=30, 0.1672 at 50, 0.1440 at 100, 0.1318 at 150 and still 0.1084 at n=300. So the verdict is NULL by the pre-committed rule and the constant is left at 30, but the honest reading is that raising it would buy very little - going from 30 to 300, a tenfold increase no live bucket could supply, moves the flip probability only from 0.185 to 0.108. THE SECONDARY IS THE STRONGER RESULT AND IT IS DEVASTATING: half-to-half sign agreement is a COIN FLIP at every bucket size tested, 0.4942 at n=30, 0.5098 at 100, 0.5212 at 150 and 0.5482 at 300, against a 0.95 target it never approaches. THAT IS NOT A SIMULATION ARTEFACT AND IT WAS CHECKED RATHER THAN ASSUMED: the book's own moments, a mean of 0.0327 and a per-trade standard deviation of 0.9251, predict agreement of 0.5059 at n=30, 0.5289 at 150 and 0.5561 at 300 in closed form, against measured values of 0.4942, 0.5212 and 0.5482. The simulation reproduces first-principles theory across the whole grid. THE NUMBER THAT MATTERS FOR EVERY FUTURE SUBGROUP CLAIM: with that mean and that dispersion a bucket needs roughly 6,148 trades before its two halves would agree on the SIGN of expectancy 95 pct of the time. The entire banked book is 3,870 and the largest live bucket, large-cap, is 2,058, with mega at 994, mid at 774 and small at 44. So per-bucket expectancy on this book is essentially unmeasurable at any floor, and the floor is doing far less work than its comment implies. This corroborates R2 and O13 from a third direction rather than adding a new claim. ZERO live buckets change status, because the verdict is NULL and the floor is unchanged. NOTHING SHIPS: MIN_CLOSED_PER_BUCKET is untouched at 30, and the item may not change live trading behaviour in any case since raising it only makes the project MORE conservative about which subgroups it will read. EXPECTATIONS scored 2 right and 3 wrong: a floor in the 75 to 150 range predicted at 70/30 is WRONG since no floor exists on the grid at all; a RAISE verdict predicted at 60/40 is WRONG since it is NULL; at least one currently-reported bucket losing enough_to_judge predicted at 65/35 is WRONG at zero; P_flip at the shipped 30 exceeding 0.15 predicted at 70/30 is RIGHT at 0.1848; and the secondary criterion being weaker than the primary predicted at 55/45 is RIGHT and by a wide margin, since the primary at least declines with n while the secondary never leaves the neighbourhood of a coin flip. Options N 247 -> 248 |
| S10 | 2026-08-11 | equity | yes | Don's own question formalized: should a top-decile name whose POINT-IN-TIME BULL case already sits at or below price at entry make the book at all. NOTE THE SCOPE, because it differs from the audit text: VALQUO_EDGE_AUDIT.md:739 specifies S10 as an ACCOUNTING red-flag veto built from Beneish M-score, Altman Z-score, combined external financing and NT late-filing notices. This register tests NONE of those four. It runs a VALUATION-BAND exclusion instead, which is a different instrument on different data, so S10's accounting half remains OPEN and the ledger row may not be closed as though Beneish and Altman had been tested | PREREG_s10_downside_exclusion.md, committed ALONE at a041e09 with no .py in the commit and a strict git ancestor of every measurement commit. Instrument is the blended bull case from pipeline._blend_scenarios, IMPORTED and never re-implemented, which is the same object the site renders as the top of its scenario card; offline beta pins it point-in-time so the WACC ladder cannot fetch a live quote for a historical valuation. FLAG is bull is not None AND bull at or below price; a name with NO computable bull case is KEPT, never excluded, because excluding on missing data is a data-availability screen wearing a valuation screen's name. ARMS on the corrected 69-date panel under the deployed flat 1/7 weights, ONE weighting and no grid: A0 incumbent, A1 DROP with the book allowed to shrink, A2 BACKFILL refilling from the next unflagged name so book size is constant, and M1 MECHANISM comparing flagged against unflagged forward returns within the decile, paired by date. DECISION RULE is the audit's own asymmetric bar restated unchanged: ADOPT-ELIGIBLE iff max drawdown improves by more than 2.0pp AND top-decile alpha falls by less than 1.0pp, in BOTH halves, boundary embargoed. TWO LIMITS STATED IN ADVANCE rather than discovered later: the 1.0pp alpha leg sits BELOW X7's calibrated 1.95pp alpha margin and survives only as a NON-INFERIORITY allowance, so a pass means no alpha loss detectable at this panel's resolution and never that the loss is under 1pp; and the 2.0pp drawdown bar is UNCALIBRATED because X7 calibrates t-statistics, alpha margins, PBO and the Deflated Sharpe and calibrates nothing whatever for drawdown | REJECTED on both arms - and the screen is worse than inert, it is counterproductive | n=3 | HANDOFF_edge_audit.md session 27; data/free_analysis/S10_DOWNSIDE_EXCLUSION.json; scripts/s10_downside_exclusion.py; scripts/s10_build_band_panel.py; valuation/engine/calibration.py; tests/test_edge.py 317 tests. COVERAGE FIRST per the COVERAGE RULE: 11,426 top-decile rows, bull-case coverage 92.42 pct, FLAGGED 3,129 rows or 27.38 pct of the decile, and the flag is far from degenerate. VERDICT: A1 DROP fails the drawdown leg and passes the alpha leg; A2 BACKFILL fails BOTH. Drawdown does not merely fail to improve, it gets materially WORSE - A1 by 2.61pp and A2 by 3.35pp on the full sample - and alpha falls 0.24pp and 0.93pp respectively. THE MECHANISM ARM IS THE REAL FINDING AND IT REFUTES THE PREMISE OUTRIGHT: within the top decile the FLAGGED names return +6.5125 pct per 63 days against the unflagged +6.2677 pct, so the names the screen would delete very slightly OUTPERFORM the names it would keep, +0.9794pp per year at HAC t +0.4775, which is a clean NULL and flips sign between halves at -0.4137pp early and +2.1161pp late. There is no information in the flag in either direction. THE AUDIT'S OWN KEY COUNT GOES THE WRONG WAY TOO: the audit argues an exclusion screen need only avoid a few catastrophes, but flagged names fall more than 50 pct in 15 of 3,129 cases, 0.479 pct, against 69 of 8,297 unflagged, 0.832 pct - the screen preferentially removes the names that crash LESS OFTEN, at roughly half the rate of the ones it retains. MECHANISM, measured on theme z-scores within the decile: flagged names carry HIGHER momentum at +0.9530 against +0.6741 and much LOWER value at +0.2728 against +0.7362, a value gap of -0.4634 that is the largest move in the table. R1's re-run puts the book on UMD +0.205 at t 3.65 and HML +0.251 at t 2.93, so the screen deletes exactly the momentum exposure R1 says is real and tilts the remainder further into value, which is the FNMA value-trap direction the free-analysis lane documented. IT IS ALSO SUBSTANTIALLY A SECTOR EXCLUSION RATHER THAN A VALUATION ONE, which is U7's failure mode in a new costume: the flagged RATE runs 51.38 pct for the financial valuation regime against 12.66 pct for hypergrowth, and by sector 48.88 pct for Financial Services and 40.32 pct for Real Estate against 15.79 pct for Industrials, a three-fold spread. An apparently valuation-based screen is in large part a decision to hold fewer banks and REITs. A DEFECT IN MY OWN INSTRUMENT, FOUND AND FIXED BEFORE ANY VERDICT WAS READ: max_drawdown is NEGATIVE, so an arm improves it by being LESS negative and the gain is arm minus base. The first cut computed base minus arm, which reported a 2.61pp WORSENING as a 2.61pp IMPROVEMENT - it would have left the REJECT verdict standing while inverting the reported reason for it, and it is the same sign error this project read into monotonicity for months. Pinned by a known-bad fixture carrying the real measured pair. A SECOND MEASURED FACT THAT LIMITS THE DRAWDOWN LEG GENERALLY: the worst peak-to-trough spans exactly ONE 63-day period on every arm, at the same trough index 44 of 69, so the entire 2.0pp drawdown criterion is decided by a single quarter, COVID 2020Q1. The register called max drawdown one order statistic; this is that caveat measured rather than asserted. CONTROLS ALL PASS: C1 the rebuilt panel reproduces S23's banked fair-value panel on all 108,241 shared keys at max abs diff 0.000e+00 across all twelve base fields with valuable, regime, method and growth_led 100.000000 pct identical, so adding the band did not disturb the base by a bit; C2 the band imports the shipped blend and is pinned; C3 ZERO ordering violations of bear at or below base at or below bull over 108,100 full trios; C5 the harness reproduces the published record to sixteen digits on top-decile alpha, long-short naive t, long-short HAC t and monotonicity, and the run aborts before reading any arm if it does not; C6 the flag is not degenerate; C7 the build is offline. SECONDARY, labelled a stronger intervention because removing flagged rows screens at continuation as well as entry: the top-25 hold book falls from 32.72 pct CAGR to 27.29 pct, and B17's caveat travels with it since that book holds a median 42 names and pays no taxes. EXPECTATIONS scored 5 right and 1 wrong, unusually good for this project and for a stated reason - they were derived from measured facts already in the record rather than from intuition. Screen costs alpha predicted at 70/30 is RIGHT; drawdown not improving by 2pp predicted at 60/40 is RIGHT and by more than predicted since it worsens; flagged fraction above 25 pct predicted at 65/35 is RIGHT at 27.38 pct; flagged names skewing high momentum and low value predicted at 75/25 is RIGHT on both legs; REJECT predicted at 70/30 is RIGHT; and the prediction that the screen would still catch a non-trivial number of genuine disasters is WRONG in the way that matters, because it catches them at HALF the rate of the names it keeps. ADOPTS NOTHING and nothing in the live scoring path changed: an entry screen is a VINTAGE EVENT and would reset the five-year forward clock, and in any case no arm is eligible. Equity N 155 to 158 for this row, three arms charged because each could independently have been reported as a positive finding; BACKTEST_RESULTS.json re-run so the Deflated Sharpe matches the honest denominator. Options N and infra N untouched, the counter being domain-scoped |
| O10 | 2026-08-11 | options | yes | The options engine charges DEFAULT_AGGRESSION of 1.0, buy the ask and sell the bid, at both ends of every trade. That constant was pre-committed results-free and has never been checked against what real orders pay. With O14's tick flow banked on the alert-days, model what a RESTING LIMIT ORDER actually fills at, and at what price, against that aggressive-fill assumption | PREREG_o10_passive_fills.md, committed ALONE with PREREG_o18_spread_cost.md at 34b0c11 with no .py in the commit and a strict git ancestor of every measurement commit. THREE SCOPE FACTS MEASURED BEFORE THE REGISTER WAS WRITTEN, from the cache and the book only and with no outcome computed first: the cache is exactly the alert-days and nothing else, so the immediate next session is cached for 0 of 3,870 banked entries and the exit day for 0 of 3,870. Therefore the LIVE order-working question, submit after the close and rest on D+1, is NOT ANSWERABLE and no verdict is issued about it, and only the ENTRY leg is coverable while a round trip crosses the spread twice. The item therefore measures the EXECUTION ENVIRONMENT of the exact contracts the book traded on the day it traded them, quote-relatively so it needs no decision time and carries no look-ahead, and it neither re-simulates the book nor reports a re-banked P and L. A resting buy limit is priced at mid plus lambda times the half-spread over a fixed grid of plus 1.0, plus 0.5, 0.0, minus 0.5 and minus 1.0, with horizons 5, 15, 30 and 60 minutes and rest-of-session, and the PRIMARY CELL is one pre-specified pair, rest at the mid over 30 minutes, the rest of the grid being a curve with no verdict. Fill price is the limit itself with no price improvement credited. THE NET PASSIVE ADVANTAGE splits into a gross saving and an ADVERSE SELECTION term, the second being the whole point, since a resting bid fills when sellers are aggressive. MATERIAL iff in BOTH halves the advantage is at least 1.00pp of entry premium AND the fill rate is at least 0.50. ROUTING FIXED IN ADVANCE: nothing is adopted whatever the verdict and a material result is routed to Don | NO VERDICT - the registered void condition C2 fired, and it disqualified the arm that would have cleared the bar | n=4 | HANDOFF_optionsbot.md section 34; data/free_analysis/O10_O18_TICKFLOW.json; scripts/o10_o18_tickflow.py; valuation/edge/tickflow.py; tests/test_tickflow.py 39 tests. The join is COMPLETE at 3,869 of 3,869 units, the one missing alert-day BUD 2024-01-10 costing exactly one book row. C1 reconciliation passes with a median relative error of 0.0233 between the book's entry_premium and the tape's last prevailing ask against a 0.05 bar, and entry_premium sits far closer to the ASK than the mid at 0.0385, which is what aggression 1.0 says it should be. C2 REQUIRED THE BEHAVIOURAL CONDITION-CODE SPLIT TO REPLICATE ON THE FULL BOOK AND IT DID NOT, because of exactly one code: code 35, 4.94 pct of prints, reads at-touch 0.2496 on the full book against 0.439 on the 120-entry probe that classified it, putting it BELOW package code 125 at 0.2649; the other four touch-seeking codes hold with room to spare at 0.437 to 0.805 against 0.135 to 0.265 for the package group. THE VOID IS NOT A TECHNICALITY AND THIS IS THE PART TO REMEMBER: the all-codes fallback that the register forbade from carrying a verdict, because it credits package liquidity to a single-leg resting order and is therefore an OPTIMISTIC bound, reads a net advantage of 1.0029pp against the 1.00pp bar, while the registered primary reads 0.6318pp. The arm disqualified in advance is exactly the one that crosses. REPORTED BECAUSE IT CUTS THE OTHER WAY: the void concealed no crossing, since the fallback's halves are 1.0760 and 0.9352 and the both-halves condition fails there too - arithmetic on published numbers and not a verdict. WHAT SURVIVES AS MEASUREMENT: at the primary cell the gross saving is 2.4555pp and adverse selection is minus 1.8237pp, so ADVERSE SELECTION EATS 74.3 PCT OF THE GROSS SAVING and the naive claim that resting at the mid saves half the spread overstates by nearly fourfold; the net advantage is 0.6318pp with a month-block CI95 of 0.5014 to 0.7643, halves 0.9159 and 0.3295, and a fill rate of 0.5726 that is monotone in the limit level at every horizon. A MEASURED BIAS IN MY OWN INSTRUMENT, reported rather than left implicit: the ask row is the instrument's own null and should read zero by construction, but reads minus 0.07 to minus 0.59pp because the reference moments that fail to fill at the ask are the late-session ones with truncated windows; the bias runs AGAINST the passive arm so it does not manufacture the null. C4 coverage is 0.7162 and clears its 0.70 bar, but the excluded 28 pct is NOT a random subsample - 62 pct wider spreads, roughly half the market cap and half the ATM open interest, and NEGATIVE expectancy of minus 3.11 pct against plus 5.82 pct for the included set - so every figure here is measured on the LIQUID part of the book and a cost model calibrated on it would understate cost exactly where it bites hardest. A PROCESS DEFECT, MINE: C2 and the outcome statistics were computed in the SAME pass, so it cannot be claimed the control was read before the numbers, and a gating control must run and be read in a separate pass. EXPECTATIONS scored 5 right and 1 wrong: adverse selection eating at least 40 pct predicted at 65/35 is RIGHT at 74.3 pct; a fill rate of at least 0.50 predicted at 60/40 is RIGHT at 0.5726; monotone fill rate predicted at 85/15 is RIGHT; the package arm showing a higher fill rate predicted at 75/25 is RIGHT at 0.6097 against 0.5726; effective below quoted predicted at 80/20 is RIGHT; and a MATERIAL verdict predicted at 55/45 is WRONG. NOTHING ADOPTED: DEFAULT_AGGRESSION untouched at 1.0 and pinned by test, and cheaper fills do NOT rescue R2 because the random-entry control is filled by the identical rule. TRIAL COST CHARGED IN FULL DESPITE NO VERDICT, against my own result: session 8's precedent is that DECLINING to run keeps the denominator, but these arms were run and measured against their bars, and a void verdict does not refund the search. Options N 248 to 252 for this row |
| O18 | 2026-08-11 | options | yes | The options engine models cost as ONE number, the full quoted half-spread at both ends. Is cost better described as a function of the OBSERVED SPREAD STATE than as a constant | PREREG_o18_spread_cost.md, committed ALONE with PREREG_o10_passive_fills.md at 34b0c11 before any measurement code existed. Incorporates O10's scope, disclosed prior knowledge, eligible-print definition, contract-day unit, 2021-03-08 half split, month-block bootstrap and C1 to C5 controls by reference, and voids with them. THE QUANTITY is rho, the effective half-spread divided by the quoted half-spread, so rho of 1 is the engine's assumption and rho below 1 means it overcharges; aggregated size-weighted to the contract-day then averaged equally across contract-days, with the unweighted version reported alongside. TWO SPREAD LEVELS ARE KEPT DELIBERATELY APART because conflating them is the obvious error: the EOD snapshot the book charges and the quote prevailing when trades actually happen. The gap between them is an AVAILABILITY term that is SELECTED, since you only avoid it if you trade when the market is there, and the register forbids quoting it as a saving; only the price-improvement term is a property of execution. SIX CONDITIONING FAMILIES fixed in advance and no others, each cut into quintiles: quoted spread-pct, entry premium, dte, market cap, minutes from the open and print size. opt_right and horizon are excluded as degenerate, the banked book being 100 pct calls and 100 pct swing. THE STATISTIC is the range of the per-quintile mean, scored against a WITHIN-BOOK LABEL PERMUTATION NULL of 2,000 draws that holds every measured value and every bin size fixed, because a raw dispersion is not evidence of anything - the error R3 already recorded once. WARRANTED iff some family's range clears its own half's p95 in BOTH halves AND the Spearman ordering agrees in sign; a family clearing full-sample but not both halves is reported and not acted on. Nothing is adopted whatever the verdict | NO VERDICT - voided with O10 by the shared C2 gate; the MEASUREMENT stands and is quotable, the verdict does not | n=6 | HANDOFF_optionsbot.md section 34; data/free_analysis/O10_O18_TICKFLOW.json; scripts/o10_o18_tickflow.py; valuation/edge/tickflow.py; tests/test_tickflow.py 39 tests. TRADES PRINT WELL INSIDE THE QUOTED SPREAD: size-weighted rho is 0.6743 with CI95 0.6617 to 0.6871 on the registered primary arm and 0.6054 on the all-codes fallback, unweighted 0.6737 and 0.5928, so the weighting does not change the story. THE DECOMPOSITION IN DOLLARS PER SHARE: the book's EOD half-spread is 0.1544, the half-spread prevailing when trades happen is 0.0999, and the effective half-spread actually paid is 0.0591. The availability term is 0.0545 and the price-improvement term 0.0408, so the apparent overcharge is 9.53 dollars per contract of which only 4.08 is defensible as an execution property. THE STRONGEST CONDITIONING IS NOT THE ONE PREDICTED: entry premium, not quoted spread. Rho falls monotonically with premium at 0.778, 0.700, 0.674, 0.626 and 0.597, with a range of 0.1812 against a permutation p95 of 0.0376, so 4.8 times the null, and a negative Spearman in both halves and both arms - a cheap option pays nearly its whole quoted half-spread and an expensive one under 60 pct of it, which is what a fixed tick on a small premium implies. F1 quoted spread-pct clears full-sample but not both halves on the primary arm; F3 dte clears nothing; F4 market cap clears full-sample only; F5 time of day clears both halves on the primary arm and not on the fallback. F6 print size is DEGENERATE and is flagged rather than reported as a failure to clear, because two of its five quintile bins are EMPTY at bin counts 0, 0, 2771, 2707 and 2490 - print size is overwhelmingly one contract so the 20th and 40th percentile edges coincide, and a bin that cannot exist is not evidence about the thing it would have measured, the same treatment O13 gave opt_right and horizon. THE O14 CAVEAT THAT DID NOT BITE, CHECKED RATHER THAN ASSUMED: HANDOFF_ticks.md warns that quote_timestamp can lag trade_timestamp by hours, which would make signed aggression meaningless, but on the traded contracts the median lag is 0.0 seconds and only 0.19 pct of prints lag by more than 60 seconds. EXPECTATIONS scored 3 right, 1 wrong, 1 split and 1 unscorable: rho below 1 predicted at 80/20 is RIGHT; size-weighted and unweighted agreeing predicted at 85/15 is RIGHT at 0.6743 against 0.6737; F5 clearing both halves predicted at 45/55 is RIGHT on the primary arm and wrong on the fallback, so SPLIT; F1 having the largest range predicted at 60/40 is WRONG since F2 is four times larger; rho falling as the quoted spread widens predicted at 70/30 is SPLIT, right in direction at a Spearman of minus 0.087 but failing both-halves; and a WARRANTED verdict predicted at 70/30 is UNSCORABLE because the C2 gate voided the verdict, though both arms would have returned WARRANTED. THE MISREADING TO HEAD OFF: rho below 1 means every options expectancy in the record is UNDERSTATED, which is NOT evidence that the options signal works, because R2's random-entry control pays the identical cost and the minus 5.0640pp gap is untouched. Options N 252 to 258 for this row |
| PT-GAPDUE | 2026-08-12 | infra | no | The forward track's recording guard demands a row for the CURRENT trading day, but a trading day's row is written only after that day's close, so a writer that has recorded everything it could possibly have recorded still reports a gap | share of trading-day mornings on which a PERFECT writer - one holding a row for every trading day since inception except the current one - reads recording_ok false; a guard a fully-compliant writer cannot satisfy is a defect whatever the share | FIXED | | track_meter.gap_report; HANDOFF_edge_audit.md session 28 |
| O3 | 2026-08-12 | options | yes | Cao-Han (2013): names with HIGHER idiosyncratic volatility earn LOWER delta-hedged option returns. The published sign is declared BEFORE any sort, so an arm that clears with the sign reversed is reported as CONTRADICTS-PUBLISHED-SIGN and is explicitly not a candidate | PREREG_o3_o4_o5_surface.md, committed ALONE at d2aa5f9 with no .py in the commit and a strict git ancestor of every measurement commit. TWO SCOPE FACTS MEASURED BEFORE THE REGISTER WAS WRITTEN. FIRST, THIS IS A SECOND LOOK AT AN ALREADY-REJECTED HYPOTHESIS AND IS CHARGED AS ONE: commit 64955ef, now on main, tested all three characteristics with the published sign declared first and REJECTED them, and the free-analysis lane concluded O3, O4 and O5 should be considered answered by that run rather than re-opened separately. The sole justification for re-opening is the deviation that lane declared in its own write-up and never closed - it used a STRADDLE, which is delta-neutral only at inception and accumulates directional exposure immediately, so its return variance is dominated by the underlying's move. This register changes the INSTRUMENT and nothing else that can be held fixed, and A1 reuses the prior lane's own idio_vol values and the panel's own strike and expiry, so A1 differs from the published rejection in the instrument ALONE. SECOND, THE FROZEN CHAINS NAMED IN THE TASK CANNOT DO THIS AND THE SUBSTITUTION IS DISCLOSED RATHER THAN MADE QUIETLY: the freeze stores a full chain only on the banked book's ENTRY dates, giving a median of 1 full-chain name per date and a maximum of 17, with 0 of 2,498 dates and 0 of 120 month-ends reaching the 20 names a quintile sort needs. The panel is therefore the prior lane's EOD-chain panel and the freeze is used for nothing. INSTRUMENT is Cao-Han's normalised delta-hedged call gain rebalanced daily, entry at the ask and exit at the bid to match DEFAULT_AGGRESSION 1.0, interior marks at the mid because they are marks and not trades, deltas from blackscholes.greeks at the IV solved from that day's mid with r 0.03 and q 0, hedge trades charged 5 bps one-way, the terminal day carried at the last solvable delta because IV is unsolvable at T equals 0, and events with fewer than 10 solvable contract-days excluded. VERDICT RULE fixed in advance: CANDIDATE iff in BOTH halves the quintile monotonicity runs in the published direction at 0.6 or better in absolute Spearman terms AND the long-short t exceeds that arm's OWN within-date label-permutation p95 AND the long-short mean is positive. The bar is the permutation null's p95, never the conventional 2.0. Ambiguous against a bar is a NULL per RUN_RULES A6 | NULL - and the direction REVERSED against the published rejection, which is the finding | n=1 | HANDOFF_optionsbot.md section 38; data/free_analysis/O3_O4_O5_SURFACE.json; scripts/o3_o4_o5_surface.py; valuation/edge/surface_xsec.py; tests/test_surface_xsec.py 26 tests. PANEL 3,373 formation events, 3,323 priceable after refusals of 43 unpriceable and 7 no-life, 117 formation dates, median 25 names per date, so the register's own void condition of fewer than 50 dates or fewer than 15 names per date did NOT fire. A1 reads n 3,289, monotonicity -0.1717, long-short mean +0.006043 with month-block CI95 0.00116 to 0.01061 over 113 blocks, t 2.5158 against its own permutation p95 of 2.016. THE VERDICT IS NULL ON TWO INDEPENDENT LEGS, not one: monotonicity misses the 0.6 bar by a factor of three in BOTH halves at -0.2315 early and -0.1169 late, and the t clears its own p95 early at 2.2884 against 2.0508 but FAILS late at 1.1509 against 2.0122. THE FINDING IS THE SIGN REVERSAL, AND IT IS WHY THE INSTRUMENT CHANGE WAS WORTH MAKING: on the straddle the prior lane measured monotonicity +0.9 with a long-short mean of -0.0770 and t -1.2142, which its own machinery flagged as contradicts_published_sign. On the delta-hedged instrument the same characteristic on the same panel sorts in the CONFIRMING direction, mean +0.006043 and t +2.5158. The published contradiction does not survive the instrument, but the confirming ordering is far too weak to clear the bar, so the honest statement is that O3 is NULL here and its prior CONTRADICTS reading should not be quoted as settled either. MECHANISM WORTH CARRYING: idio_vol is within-date rank-correlated +0.8444 with the option's own ATM implied vol, so A1 is close to a pure implied-vol sort and high idio-vol means expensive option. THE INSTRUMENT BEHAVES AS THE LITERATURE SAYS IT SHOULD, which is corroboration that it is real rather than a bug: the mean delta-hedged gain is -0.0072 and EVERY quintile of EVERY arm is negative, the volatility risk premium stylised fact. Options N 258 to 259 for this row |
| O4 | 2026-08-12 | options | yes | Boyer-Vorkink (2010): names with higher EXPECTED idiosyncratic skewness earn lower subsequent returns, tested here on delta-hedged option returns with the published sign declared before any sort | Same register, PREREG_o3_o4_o5_surface.md at d2aa5f9, same instrument, same panel, same verdict rule and same void conditions as O3, which are incorporated by reference and are not restated. A2 CHANGES THE CHARACTERISTIC AS WELL AS THE INSTRUMENT AND THAT IS STATED PLAINLY IN THE REGISTER: the prior lane sorted on REALISED idiosyncratic skew where Boyer-Vorkink specify an EXPECTED one. The characteristic here is the FITTED value of a cross-sectional OLS of realised idio skew on lagged idio_skew, idio_vol and 6-month momentum, fitted expanding on data available at each formation date and never on the future. The predictor set is fixed in the register before any fit was run. Boyer-Vorkink's own construction uses a larger predictor set and industry effects, so this is a SIMPLIFICATION and is labelled one rather than presented as their specification. The prior lane's realised-skew definition is re-run on the new instrument as a DISCLOSED COMPARISON carrying no verdict | NULL - and it fails its own bar more narrowly than any other arm, by 0.0086 of a t | n=1 | HANDOFF_optionsbot.md section 38; data/free_analysis/O3_O4_O5_SURFACE.json; scripts/o3_o4_o5_surface.py; valuation/edge/surface_xsec.py; tests/test_surface_xsec.py 26 tests. A2 reads n 3,154 events over 108 dates, monotonicity -0.0380, long-short mean +0.004795 with CI95 0.00008 to 0.00984, t 1.9143 against its own permutation p95 of 1.9229. THE FULL-SAMPLE t MISSES ITS OWN CALIBRATED BAR BY 0.0086, which is exactly the case RUN_RULES A6 exists for - ambiguous against a threshold is a NULL, and this row is recorded as one rather than rounded into a pass. It is ALSO null on the monotonicity leg by a wide margin and in both halves at -0.0712 early and -0.0071 late, and on the both-halves t leg, clearing early at 2.2219 against 1.9608 and failing late at 0.4201 against 1.9201. Three independent legs fail, so nothing turns on the 0.0086. THE PRE-REGISTERED EXPECTATION WAS WRONG IN THE INFORMATIVE DIRECTION: E4 predicted the expected-skew arm would be WEAKER than the prior lane's realised-skew arm, and on the same instrument and panel the expected version reads t 1.9143 against the realised version's 1.5805. Using the construction the literature actually specifies is the stronger of the two, which is a reason to prefer it in any future register even though neither clears. Options N 259 to 260 for this row |
| O5 | 2026-08-12 | options | yes | Vol-of-vol risk premium: names whose own implied volatility is itself more volatile earn lower delta-hedged option returns, published sign declared before any sort | Same register, PREREG_o3_o4_o5_surface.md at d2aa5f9, same instrument, panel, verdict rule and void conditions as O3, incorporated by reference. A3 CHANGES THE CHARACTERISTIC AS WELL AS THE INSTRUMENT, DISCLOSED IN THE REGISTER: vol-of-vol is the standard deviation of daily log changes in the name's own ATM implied volatility over 63 sessions, computed on THE TENOR THE INSTRUMENT ACTUALLY TRADES rather than the prior lane's 60-DTE series, which its own write-up listed as a deviation because its instrument was roughly 30 DTE. The prior lane's 60-DTE definition is re-run on the new instrument as a DISCLOSED COMPARISON carrying no verdict | NULL - the strongest long-short t of the three arms and still NULL, on two independent legs | n=1 | HANDOFF_optionsbot.md section 38; data/free_analysis/O3_O4_O5_SURFACE.json; scripts/o3_o4_o5_surface.py; valuation/edge/surface_xsec.py; tests/test_surface_xsec.py 26 tests. A3 reads n 3,318 events over 113 dates, monotonicity -0.0690, long-short mean +0.006299 with CI95 0.00226 to 0.01065, t 2.9703 against its own permutation p95 of 1.9459 - the largest margin over a calibrated bar anywhere in this register. IT IS STILL A NULL AND THE REASON MATTERS: monotonicity is -0.069 against a 0.6 bar and misses in BOTH halves at -0.0093 early and -0.1237 late, and the t itself FAILS the early half at 1.8189 against 1.8877 while clearing the late half at 2.5231 against 1.9431. A spread that is significant full-sample, absent in one half and unaccompanied by any quintile ordering is the shape this project has now recorded repeatedly, and it is not a signal. TENOR ALIGNMENT IS MATERIAL AND THE EXPECTATION THAT IT WOULD BE IS THE ONE THAT PAID: on the identical instrument and panel the tenor-aligned series reads t 2.9703 against the prior lane's 60-DTE series at 1.1236, so measuring vol-of-vol at a tenor the instrument does not trade materially understates it. A DIAGNOSTIC RUN AFTER THE ARMS WERE READ AND LABELLED AS SUCH, carrying no verdict and no trial cost, because the three arms share a shape the register did not anticipate - Q5 is the WORST delta-hedged bucket in all three, while Q1 to Q4 are unordered, which is precisely how a significant long-short coexists with near-zero monotonicity. THE OBVIOUS EXPLANATION IS REFUTED: the three arms are NOT one effect wearing three names, since idio_vol and vol_of_vol are NEGATIVELY rank-correlated within date at -0.2920 and share only 14.0 pct of their Q5 membership. THE SECOND OBVIOUS EXPLANATION IS ALSO REFUTED: it is not an illiquidity artefact, because the liquidity gradient runs in OPPOSITE directions - idio_vol's Q5 is the most liquid corner at a mean spread of 0.0764 against 0.1063 in Q1, while vol_of_vol's Q5 is the least liquid at 0.0980 against 0.0809. Why three largely independent sorts all put their worst bucket at Q5 is left OPEN and needs its own register; testing it here would be the fourth arm the register's own void condition 3 forbids, and declining keeps the denominator per session 8. A DEFECT REPORTED OUTSIDE THIS LANE, per RUN_RULES rule 3: in the prior lane's panel.pkl the illiq and spread_pct columns are THE SAME COLUMN, identical on all 3,373 rows. That lane's illiq was the only characteristic in its published run with an absolute t above 2, at 2.46, and is described as a mechanical liquidity control; it is the option's quoted spread percentage under a second name. Reported, not repaired - not this lane's file. Options N 260 to 261 for this row |
| S3 | 2026-08-12 | equity | yes | The insider theme is the only negative-IC theme still carrying weight, and its construction could explain that: the +min(10, 2*buys) bonus is unconditionally additive so a name with five large sales and one trivial buy still receives it, and tanh(net/5e6) saturates so a 50M cluster buy and a 15M one are indistinguishable regardless of company size. Rebuild it per the audit's three variants | PREREG_s3_insider_rebuild.md, committed ALONE at b3a85fa with no .py in the commit and a strict git ancestor of every measurement commit. ONE panel build, FOUR scorings, all arms columns on one frame so the row set is identical by construction. A0 incumbent; S3A drops the buys bonus; S3B scales net dollars by market cap before the tanh at a pre-committed 10bp scale; S3C splits the theme into two z-scored inputs, net and breadth. A name with no insider rows scores None in every arm and is never imputed a neutral 50. PRIMARY GATE is the shipped holdout_compare_panels at the already-committed margins, +0.25 long-short t AND +100bps top-decile alpha in BOTH split directions, boundary embargoed, under the deployed flat 1/7 weighting only, no grid. THE AUDIT'S OWN +1.0 theme-IC BAR IS DEMOTED IN ADVANCE to a diagnostic that may never carry a verdict, because it sits far below X7's calibrated 2.71 and because theme IC is the wrong instrument for a construction change by this project's own repeated finding | ALL THREE VARIANTS REJECTED - and the audit's own bar would have rejected them too, for a reason unrelated to what the composite did | n=3 | HANDOFF_edge_audit.md session 29; data/free_analysis/S3_INSIDER_REBUILD.json; scripts/s3_insider_rebuild.py; tests/test_edge.py |
